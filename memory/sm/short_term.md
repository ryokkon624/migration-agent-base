# SM 短期記憶（今スプリント）

Sprint 23 完了。次スプリント開始時にリセット済み

## 次スプリントへの申し送り

1. **teammate の完了は idle 通知でなく、本人報告＋成果物の直接確認**（memory/Discord/Issue/branch）で判断する。**Sprint 23 で4回目の有効事例**（PO の1回目の idle 時、`git diff --stat` と `grep -c` で依頼が未着手と判明 → nudge して反映を待った）
2. **数値は一次データで検算する。ただし (a) 検算の前に「機構出力か手計算か」を生成元で確認し、(b) 「食い違う」と言う前に A と B が同じ対象を指しているか確認する**（(b) は Sprint 23 で ④ へ昇格）。**SM 自身の verification も false positive を出す**
3. **自分が書いた短期記憶でも、要約から引かず一次データに当たる**（Sprint 23＝「L2 は理論上限に到達済み」という要約が、Sprint 22 の記憶にあった「スコープ外／構造的に到達不能」の区別を潰していた）
4. **SM が先にブランチを切って渡す**（3スプリント連続で main 直コミット再発ゼロ）
5. **teammate に渡す「範囲」の指定漏れに注意**（Sprint 23＝観測点の探索対象を2リポジトリと指定したが、`jpetstore-database` を含めないと穴を3件水増しするところだった。DEV が確認してきて助かった）
6. **破壊的な雑務は、それが一次データでないか確認してから実行する**（Sprint 23＝「掃除してよい」と言われた `out/out2/out3` がゲート値の唯一の裏取り経路だった）
7. **CRLF の落とし穴**（PO 申し送り）: Git Bash の `awk`/`sed` はこのリポジトリの CRLF ファイルを LF に落とす。行移動・行挿入を script でやるときは `ORS="\r\n"` 指定か最後に復元し、**必ず `file` で確認**する
8. **MCP（特に Discord）の疎通をセッション開始時に確認する**。Sprint 23 は全期間で疎通あり。⚠️ 未対応の潜在バグ: `discord` の env 変数名がプロジェクト `.mcp.json`=`DISCORD_TOKEN`（正）／ユーザー `~/.claude.json`=`DISCORD_BOT_TOKEN`（誤）で食い違う（**ユーザー判断待ち・Sprint 22 から持ち越し**）
9. **コード差分ゼロのスプリントの reviewer 運用は未確定**（Sprint 23 初出）。今回は 3 reviewer を起動せず SM の独立検算で代替し、理由を PR body と Discord に明記した。**Retro でユーザー判断を仰いでいる**

## Phase 4 完走後の状態（次スプリントの前提）

- **Phase 0〜4 の一気通貫が完成した。** 最終成果物 `reports/after/verification-report.md`
- **意図差分台帳は 33 件**（L4 で ID-32・ID-33 を追記。末尾に `## 訂正履歴` 節）
- **L2 ゲート値は据え置き**: BRANCH ≥ 28/34・INSTRUCTION ≥ 1360/1424（`gate-v2` 分母）。**#55 でシナリオ集合が変わるのでそこで再合意する**
- **「28/34 は理論上限」は撤回済み**。残6は4種類（構造的に到達不能2・seed 前提で到達不能1・**到達可能だがスコープ外3**）。到達可能な3つを踏めば 31/34 の見込みだが**実測が正**
- **`tools/legacy-jacoco/out{,2,3}/` は削除しない**（ゲート値の唯一の裏取り経路。gitignore 済みで未コミット）

## 次の Planning で扱う候補

- **#54**（L4 由来・観測点の穴を埋める＝ID-30・ID-33）／**#55**（L4 由来・部分観測の補強と退行検知器）— いずれも Ready=Draft。**SP とプロダクトバックログ上の優先順位はユーザーが決める**
- **#42〜#47（SEC Low 束）の Ready 昇格トリガが充足した**（PO 申し送り）: 昇格条件のうち「Phase 4 完了後に容量が空いた時点」が **Phase 4 完走により満たされた**（#42・#44・#43(C)(D)・#46・#47）。次回 Refinement で PO が判定する
- **#32**（登録時のメールアドレス所有権検証）は NotReady のまま
- **Challenge**: `verification-strategy.md` §6 が言う「記事③」の材料が揃った。`verification-report.md` は「（中身は知らないが）何も変わっていないと証明しろ」への直接の回答としてそのまま使える

## 未解決の持ち越し（PO 申し送り）

1. **ID-26 の版乖離（backend 9.5.0 / database 26.7.0）の据え置き理由が未記録** — 依存更新の判断として別途扱う
2. **在庫枯渇の再評価タイミング** — 本番基盤の整備決定時に #45・#43(B)(E)・#46 と束ねる
3. **注文の二重送信は条件付き保留** — 実測で「旧は二重送信で1件しか作らない」等の反証が出たら台帳行へ切り替える
4. **ID-22 は「観測点はあるが宣言どおりに守られていない」状態**（S20-4 の fail-open 化を Low 受容）。#47(C) 後の関連Story欄拡張の判定時にはこれを前提にする
