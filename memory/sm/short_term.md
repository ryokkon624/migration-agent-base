# SM 短期記憶（今スプリント）

Sprint 22 完了。次スプリント開始時にリセット済み

## 次スプリントへの申し送り

1. **teammate の完了は idle 通知でなく、本人報告＋成果物の直接確認**（memory/Discord/Issue/branch）で判断する
2. **push はセッション単位で揺れる**（⑥ のはしご）。**代行の前に `rules/git.md` 上の正当性を確認**する。マージは `mcp__github__merge_pull_request`
3. **SM が先にブランチを切って渡す**（Sprint21 是正 → Sprint22 適用で main 直コミット再発ゼロ・2回連続有効）
4. **数値は一次データで検算する。ただし検算の前に「機構出力か手計算か」を生成元で確認する**（Sprint22 で ④ にガード節追加。SM が機構出力を手計算と誤認して差し戻した実績）
5. **下流へ渡す「訂正リスト」も現物で検算する**（Sprint22＝Issue 本文に無い値を「本文の訂正」として PO へ渡し、PO が現物確認で訂正。SM 作業物〔backlog〕と Issue 本文を混同しない）
6. **セッション開始時に MCP（特に Discord）の疎通を確認する**（Sprint22 は全期間不通で3スレッド未投稿→事後バックフィルで解消）。⚠️ 未対応の潜在バグ: `discord` の env 変数名がプロジェクト `.mcp.json`=`DISCORD_TOKEN`（正）／ユーザー `~/.claude.json`=`DISCORD_BOT_TOKEN`（誤）で食い違う（**ユーザー判断待ち**）。Discord は**1メッセージ2000文字上限**＝分割投稿が前提
7. **検証資産 Story では耐久性要件を計画フェーズで先渡しする**（① へ昇格）。**前提 assert は比較の両側に対称に置く**（④ へ昇格）

## Sprint 22 の確定値（次スプリントの前提）

- **ゲート値（PO 合意済み）**: BRANCH **≥ 28/34（82.4%）**／INSTRUCTION **≥ 1360/1424（95.5%・`gate-v2` 分母）**
- **実測 28/34 は到達可能な理論上限ちょうど**。残6＝`SqlMapItemDao` 3・`SqlMapSequenceDao` 1（スコープ外）／`Cart` 1・`CartItem` 1（構造的に到達不能）
  → **L2 の次イテレーションは「分岐を増やす」方向では伸びない**。観測点の質（台帳 ID に対する観測点の有無）へ軸を移す検討が必要（Retro C2）
- **Issue #51 本文の訂正4件は PO が次回 Refinement で一括対応**（W5 ケース数 2→3／R8b 404→403／AC-neg4 のログインパス／スコープ表 R8a の expectation）
