# SM 短期記憶（今スプリント）

## Sprint 23（進行中 → Sprint Review 待ち）

**ゴール**: Phase 4 の締め＝L4（意図差分台帳の照合）。`reports/after/verification-report.md` を作る。
**対象**: #52（8SP）・#53（1SP）／**ブランチ**: `docs/52-l4-ledger-verification`（SM が事前作成）
**PR**: https://github.com/ryokkon624/migration-agent-base/pull/25 （closes #52 / #53）
**Sprint Review**: https://claude.ai/code/artifact/6b27a2f4-08a7-433e-af97-6e3a60c6ea20

### 結果

- 対応表 **33行**（台帳が 31 → 33 件に増えたため）: 観測点あり 31（うち部分観測 6）／穴 2（ID-30・ID-33）／構造的に観測不能 0／観測不要 0
- ゲート: L1 PASS（951テスト・failures 0）／L2 PASS（parityTest 23/23・BRANCH 28/34・INSTRUCTION 1360/1424）／L3 PASS（S1〜S21 の 21/21）／**L4 是正後 PASS**
- **旧新の直接比較（L2）が成立しているのは 33 件中 4 件だけ**（ID-1・14・24・29）← 本 Sprint 最大の発見
- 次イテレーションを **#54**（穴を埋める）・**#55**（部分観測の補強）として起票

### 今スプリントで有効だった進行

1. **PO と DEV を「仕様側判定」と「実測」で分担させた**。PO＝台帳オーナーとして「比較が成立するか」、DEV＝「観測点を実在するテスト名で名指しできるか」。**判定を先に決めさせず、両者の突き合わせで SM が決めた**ので、水増しも取り繕いも起きなかった。
2. **計画フェーズで「名指しの規則」を AC に書いた**（L2＝シナリオID＋宣言文字列＋golden／L3＝S番号＋根拠種別＋何を観測しているか／L1＝実在する Spec の実在するテスト名＋file:line）。これが無いと「観測点あり」が水増しになる。
3. **`idle_notification` で完了判断しなかった**。PO の1回目の idle 時、`git diff --stat` と `grep -c` で成果物を直接確認したところ**依頼1〜3が未着手**だった。nudge して反映を待った（長期記憶の運用が3回目も有効）。
4. **teammate の反証を受け入れた**。SM の U5（ID-26 の CVE 食い違い）は誤りで、PO の反証が正しかった。**座標（どのリポジトリの、どの依存か）まで確認せずに「食い違い」と呼んだ**のが原因。
5. **DEV の範囲外れの提案を採用した**。SM が対象リポジトリを2つと指定したが、DEV が「ID-7・21・23 の観測点は `jpetstore-database` にしかない。除外すると穴を3件水増しする」と指摘。**指定漏れを認めて範囲を広げた**。

### 次スプリントへの申し送り

1. **teammate の完了は idle 通知でなく、本人報告＋成果物の直接確認**（4回目の有効事例）
2. **数値は一次データで検算する。ただし検算の前に「機構出力か手計算か」を生成元で確認する**（Sprint22 のガード節）。**さらに今回追加: 「食い違い」と呼ぶ前に座標（どのリポジトリ・どの依存・どの表）まで確認する**（V11）
3. **SM が先にブランチを切って渡す**（3回連続で main 直コミット再発ゼロ）
4. **コード差分ゼロのスプリントでは 3 reviewer を起動しない判断をした**。理由を PR body と Discord に明記した。異論があれば次回から運用を変える
5. **CRLF の落とし穴（PO 申し送り）**: Git Bash の `awk`/`sed` はこのリポジトリの CRLF ファイルを LF に落とす。行移動・行挿入を script でやるときは `ORS="\r\n"` 指定か最後に復元し、**必ず `file` で確認**する。放置すると全行が diff に出る
6. **Discord は今回は全期間で疎通あり**（Sprint 22 は全期間不通だった）。セッション開始時の疎通確認は継続する。⚠️ 未対応の潜在バグ: `discord` の env 変数名がプロジェクト `.mcp.json`=`DISCORD_TOKEN`（正）／ユーザー `~/.claude.json`=`DISCORD_BOT_TOKEN`（誤）で食い違う（**ユーザー判断待ち・持ち越し**）

### Sprint 23 の確定値（次スプリントの前提）

- **台帳は 33 件**（ID-32・ID-33 を L4 で追記。訂正履歴節つき）
- **L2 ゲート値は据え置き**: BRANCH ≥ 28/34・INSTRUCTION ≥ 1360/1424（`gate-v2` 分母）。**#55 でシナリオ集合が変わるので、そこで再合意する**
- **「28/34 は理論上限」は撤回済み**。残6は4種類（構造的に到達不能2・seed 前提で到達不能1・**到達可能だがスコープ外3**）。到達可能な3つを踏めば 31/34 の見込みだが**実測が正**
- **`tools/legacy-jacoco/out{,2,3}/` は削除しない**（`reports/after/l2-parity-coverage.md` が参照する一次データ。gitignore 済みでコミットされていない）
- **未解決の持ち越し**: ID-26 の版乖離（9.5.0 / 26.7.0）の据え置き理由が未記録／在庫枯渇の再評価タイミング（本番基盤の整備決定時に #45・#43・#46 と束ねる）／注文の二重送信は条件付き保留（実測で反証が出たら台帳行へ）
