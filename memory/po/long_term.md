# PO 長期記憶（過去スプリントの教訓）

## SMやDevからの質問傾向

### Sprint 1（2026-08-13）— E6基盤系（#22 DB移行）計画フェーズより

DEVからSprint1計画フェーズでユーザーに直接確認した事項をSM経由で中継され、5件を質問ログに記録した（全て初出のトピックのため、本Retroでは個別のチェックリスト昇格は見送り、傾向記録に留める）。

- 命名規約（m_/t_プレフィックス等）が spec に明文化されていない
- 代理キー（surrogate key BIGINT）導入の是非・WHO前提との整合
- orderstatus表廃止＋status_code列への統合という具体的な統合方式
- Sprint1のseed範囲（DDLのみかバルクseed含むか）
- （参考・バックログ修正不要と判定）DB専用リポジトリでのTDD検証手段＝Testcontainers採用の是非。実装手段の選択はDEV裁量の領域に該当。

**傾向**: Acceptance Criteria は DDL の"何を"（テーブル・列・制約）は書くが、命名規約・PK戦略・既存表の統合/廃止方式・seed範囲といった"どう作るか/どこまでやるか"の基盤決定が未定義になりがちで、実装直前の確認に集中した。5件中4件はAC/備考への先回り明記で防げた可能性があった。

**所感（次回Refinementでの適用方針）**: E6基盤系Story（DB移行・backend/frontend土台など新規リポジトリ着手のStory）を起票・詳細化する際は、Refinement段階で以下の観点をAC/備考に先回り明記できないか確認する。ただし本トレンドは今スプリントが初出のため、正式なチェックリスト項目への昇格は次回以降に同種の質問が出た場合に判断する。

- テーブル・列の命名規約（プレフィックス・大小文字規則）
- 主キー戦略（surrogate/natural key、型。WHO列などの前提整合）
- 既存テーブルの統合・廃止方針（対象列名まで具体化して明記）
- Sprint内のseed/fixtureスコープ（DDLのみか、バルクデータ投入まで含むか）

### Sprint 2（2026-08-14）— E6基盤系（#23 バックエンド・アーキ土台）計画フェーズより

DEVからSprint2計画フェーズでユーザーに直接確認した事項をSM経由で中継され、6件を質問ログに記録した（1件はDEV裁量と判定・5件はバックログ修正候補）。

- 土台Story(#23)のスコープ境界（基盤とドメインStoryの線引き。ドメインservice/endpointを作らない方針・送り先Issue番号）
- 未実装の依存機能がある状態での実証手段（ログインEP不在下でのJWT+refresh実証方法）
- 監査ログの記録範囲（WHO行レベルとaudit_logイベントレベルの責務分離、本Storyでどこまで結線するか）
- 秘密情報管理の厳格さ（fail-fast方針かデフォルト値許容か）
- 統合テストでの他リポジトリ資産（jpetstore-databaseのFlyway）の適用方式
- （参考・バックログ修正不要と判定）jjwtライブラリの版更新要否。ライブラリバージョン選定はDEV裁量の領域に該当。

**傾向**: Sprint1（DB移行#22）で観測した「ACはWHAT（テーブル・列・制約など機能要件）は書くが、"どう作るか/どこまでやるか"の基盤決定が未定義になりがち」という傾向が、Sprint2（backend/frontendアーキ土台#23）でも別の切り口（スコープ境界・実証手段・秘密管理・外部資産連携）で再現した。6件中5件がAC/備考への先回り明記で防げた可能性があった。

**判定**: 2Sprint連続で同種の傾向（E6基盤系Story特有の"どう作るか/どこまでやるか"未定義）が発生したため、下記チェックリストへ正式昇格する。

### Sprint 3（2026-08-15）— E5系（#18 サインオン/サインオフ）計画フェーズより

DEVからSprint3計画フェーズでユーザーに直接確認した事項をSM経由で中継され、1件を質問ログに記録した。

- #18（E5 サインオン/サインオフ）の認証UI（Vue3 SPAログイン画面・元URL復帰UX）のスコープ境界。AC1「JSP廃止・Vue3 SPA」・AC3「元URL復帰」の文言が、backend土台StoryでありながらフロントUI責務まで含むように読める書き方だったため、backend REST＋Spring Security基盤に絞るかフロントUIまで含めるかの確認が発生した。回答：backendに絞り、フロント責務（Vueログイン画面・Piniaauthストア・ルーターガードでの元URL復帰・SPA配送）は#24（E6フロント土台・Sprint5予定）へAC7・AC8・AC-neg2として持ち越し。

**傾向**: Sprint1(#22)・Sprint2(#23)で観測した「E6基盤系Story」のスコープ境界問題とは異なる切り口で、E5系のfeature（ドメイン）Storyでも「AC文言がbackend/frontend両責務にまたがる書き方になっていると、どこまでが当該Storyの範囲か・持ち越し先はどこかが計画フェーズで問われる」という事象が発生した。

**判定**: 今回が初出のため、正式チェックリストへの昇格は見送り、傾向記録に留める。次回以降、feature Story（ドメインStory）で同種の質問（backend/frontend責務またぎのスコープ境界）が2回目以降発生した場合に、下記の観点を軸に正式昇格を検討する：

- Vue3 SPA化を伴うfeature Storyは、起票時点でbackend責務（API・セキュリティ基盤）とfrontend責務（画面・ストア・ルーティング）を別ACまたは別Storyとして明示的に切り分けられないか

### Sprint 4（2026-08-15）— E5系（#21 認可土台・#20 認証堅牢化）計画フェーズより

DEVからSprint4計画フェーズでユーザーに直接確認した事項をSM経由で中継され、6件を質問ログに記録した。

- #20 レート制限/ロックアウトの試行回数・ロック状態の保持先（DB表 vs in-memory）＝cross-repo（jpetstore-database）に及ぶ設計判断
- #20 既定資格情報プリフィル廃止（フロント責務）を今スプリントで扱うか＝#24へAC化して持ち越し
- #20 SBD-9オープンリダイレクト対策として、sink不在のbackendに予防的allowlistガードを足すか＝足さず構造的解消（sink不在の明文化＋回帰テスト）とする
- #21 認可土台のスコープ境界（適用先ドメイン未実装の中でどこまで作るか。ロールモデル整備の要否）
- #21 AC2の認可失敗監査記録は既存機構（recordAuthzFailure）で足りるか
- #20 レート制限の粒度（per-IPか）・閾値をいくつにするか

**傾向1（新規パターン・2件・同一Sprint内で発生）＝「secure-by-default機構の既達判定」**: #20のSBD-9（予防的ガードを足すか）と#21のAC2（監査記録は既存機構で足りるか）は、いずれも「新規にセキュリティ対策を追加すべきか、既存実装が既に要件を満たしているか」の判定を計画フェーズで求められた点で同型。ACが「対策を新設せよ」と読める書き方になっており、既存資産で達成済みである可能性がAC/備考に明記されていなかったため発生した。

**傾向2（Sprint3 #18と同型・2回目の発生）＝「backend/frontend責務またぎの持ち越し＝AC化」**: #20のプリフィル廃止（フロント責務）が#24へ持ち越しとなった件は、Sprint3 #18で観測した「backend中心StoryにfrontendUIが混入し、範囲外化・持ち越し先の明記が必要になる」パターンの再発（同一送り先#24）。

**傾向3（既存チェックリスト項目の3回目の再発）＝「土台Storyのスコープ境界」**: #21のスコープ境界確認は、Sprint2 #23で正式昇格済みの「スコープ境界」チェックリスト項目と同型。既にチェックリストに存在する観点が#21のRefinementでは適用されなかった（または#21が「E6基盤系Story」という分類に当てはまらず対象外と判断された）ため再発した可能性がある。**チェックリストの適用範囲を「E6基盤系Story」に限定せず、認可/認証基盤など横断機構に着手するStory全般へ広げる必要がある**（下記チェックリスト更新済み）。

**傾向4（新規・1件・初出）**: #20のレート制限粒度・閾値（per-IP/per-username・具体的な回数と期間）はAC1に定性的表現（「レート制限/ロックアウトを設ける」）のみで定量パラメータが書かれておらず確認が発生した。初出のため傾向記録に留める。

**判定**: 傾向1・傾向2は2回以上の発生条件を満たすため、下記チェックリストへ正式昇格する。傾向3は既存チェックリスト項目の適用範囲を広げる形で反映する。傾向4は次回以降の再発時に昇格判定する。

### Sprint 5（2026-08-15）— E6基盤系（#24 フロント・アーキ土台）計画フェーズより

DEVからSprint5計画フェーズでユーザーに直接確認した事項をSM経由で中継され、6件を質問ログに記録した（全件バックログ修正候補）。

- #24 認証状態のリロード再水和方式（backendに`/me`相当APIが無く、フロントのidentityがリロードで揮発する問題）
- #24 AC8（保護ルート→ログイン→元URL復帰）の実証にライブの保護デモ画面が必要か
- #24 AC3新規画像アセットのスコープ（foundationとドメインStoryの切り分け）
- #24 i18nライブラリ／キー構造（横断的な技術選定）
- #24 共通コンポーネントの範囲（過剰な共通化の回避）
- #24 本スプリントで作る画面の範囲（Home/Signon/Signoffとドメイン画面の切り分け）

**傾向1（既存チェックリスト項目の3回目の再発）＝「スコープ境界」「実証手段」**: スコープ境界（画面範囲・共通コンポーネント範囲・画像アセット範囲の3件）と実証手段（AC8の1件）が、Sprint2 #23→Sprint4 #21に続きSprint5 #24でも発生した。土台Storyの「範囲」「実証手段」をAC/備考に先回り明記する運用が3スプリント連続で徹底されていない。**チェックリストの記述を強化**し、「画面単位・コンポーネント単位・アセット単位など、Story種別に応じた具体的な範囲区分」まで明記するよう説明文を補強する（下記チェックリスト更新済み）。

**傾向2（新規パターン・初出）＝「フロント土台Storyでのbackend API過不足の未確認によるcross-repo追加」**: 認証状態再水和の要否・方式が#24起票時点で未定義だったため、計画フェーズでbackendへの機能追加（`GET /api/auth/me`、cross-repo）が決定した。Sprint4 #20（レート制限のDB表保持=cross-repo判断）とは切り口が異なる（データ保持先 vs API過不足）ものの、「土台/機能Story起票時にcross-repo波及の要否確認が漏れる」という上位パターンの点で近い。本パターンとしては初出のため、正式昇格は見送り傾向記録に留める。次回以降、同種（frontend土台起票時のbackend API過不足未確認）が発生した場合に昇格を判断する。

**傾向3（新規パターン・初出）＝「横断ライブラリの技術選定/命名がAC未定義」**: i18nライブラリ・キー構造という、今後全画面に波及する技術選定がAC/備考に書かれておらず確認が発生した。土台Storyで導入する横断ライブラリ（状態管理・i18n・HTTPクライアント等）の採用ライブラリ名・バージョン・命名規則をAC/備考に先回り明記できないか、という観点。初出のため傾向記録に留める。

**判定**: 傾向1は3回目の再発のため、既存チェックリスト項目（スコープ境界・実証手段）の説明文を強化する（正式昇格は既に完了済みのため再昇格は不要）。傾向2・傾向3は初出のため、次回以降の再発時に正式昇格を判断する。

### Sprint 6（2026-08-16）— E1系（#1 カタログ階層閲覧・初のドメイン機能）計画フェーズより

DEVからSprint6計画フェーズでユーザーに直接確認した事項をSM経由で中継され、3件を質問ログに記録した（全件バックログ修正候補）。本プロジェクト初のドメイン機能Storyであり、いずれも「初のドメイン機能で確立する先例・規約」に関わる論点だった。

- #1 アイテム詳細の在庫バッジ「残少（Low Stock）」閾値N（レガシーは全在庫qty=10000固定で残少概念なし＝新規UX値。AC3に定量値の記載なし）
- #1 在庫ステータス（在庫あり/残少/在庫切れ）の実装方式（手書きdomain enum vs m_code区分値登録）
- #1 一覧APIのページ採番（1-index vs 0-index。#2/#9が再利用する先例規約）

**傾向1（Sprint4 #20傾向4と同型・2回目の発生）＝「定量パラメータ未定義」**: #1の残少バッジ閾値Nは、Sprint4 #20（レート制限の粒度・閾値）と同様、ACに定性的表現（「バッジで表示」）のみで定量値が書かれておらず確認が発生した。新規UX値のためRefinement時点でPOが具体値を決め切るのは難しい面もあるが、2Sprintにまたがる同型の再発のため下記チェックリストへ正式昇格する。

**傾向2（Sprint5 傾向3と同型・2回目の発生）＝「先例規約未定義」**: #1のページ採番確認は、Sprint5 #24（i18nライブラリ/キー構造という横断技術選定がAC未定義）と同様、「初のドメイン機能/土台Storyが後続Story群の再利用する先例規約を確立するにもかかわらず、その規約が事前にAC/備考へ具体的な確定値として書かれていない」パターンの2回目の発生。Issue #1自体には「1-indexか0-indexかも確定する」と論点として明記されていた（確認の発生自体は元々想定されていた）が、確定値そのものがAC化されていなかった点は同型のため、下記チェックリストへ正式昇格する。

**傾向3（新規パターン・初出）＝「区分値のm_code採用に関する一般方針の未文書化」**: #1の在庫ステータス実装方式確認で、ユーザーから「区分値は基本的にm_codeに登録する」という一般方針が明示された。architecture-conventions §3.1は在庫ステータスをm_code候補として例示しつつ「確定はPO/仕様で」としており、今回はPO/仕様の確定としてm_code採用と扱った。§3.1自体がPO判断を要求する設計のため、Issue起票時点の先回り明記だけでは確認を防げなかった可能性がある一方、一般方針として§3.1をfirmupすれば今後は同種の質問が発生する前にPOが自律的に判断できる。今回が初出のため正式昇格は見送り、architecture-conventions §3.1のfirmup（「区分値は基本的にm_codeに登録する」の明文化）要否は次回同種発生時に判断する。

**判定**: 傾向1・傾向2は2回目の発生条件を満たすため、下記チェックリストへ正式昇格する（Sprint5傾向2〔frontend土台でのbackend API過不足未確認によるcross-repo追加〕は本Sprintでの再発なし・引き続き監視）。傾向3は初出のため、次回以降の再発時に判断する。

### Sprint 7（2026-08-16）— #2（検索・カテゴリフィルタ）計画フェーズより

Sprint7計画フェーズで、SMがユーザー承認取得した2件（specがPO/仕様に委譲していた論点）とDEVがユーザーに直接確認した5件を中継され、計7件を質問ログに記録した（1件はAC1に元々「PO決定」と明記済みで意図どおりの確認・1件は環境衛生でバックログ修正不要・5件がバックログ修正候補）。Epic E1（カタログ）はSprint7で完成。

- #2 検索語のLIKEメタ文字（`%`/`_`）の扱い（specがPO/仕様に確定を委譲）→ハードニング採用
- #2 AC1「カテゴリフィルタは任意（PO決定）」の実装要否→今スプリントで実装（AC1に既に「PO決定」と明記済みの想定どおりの確認）
- #2 検索APIのパス設計（`/api/catalog/search` か `/api/products/search` か）
- #2 カテゴリフィルタのUI配置（ヘッダ or 検索結果画面）
- #2 検索対象列（カテゴリ表示名を含めるか）
- #2 不明な`categoryId`フィルタ時のHTTPステータス（404 か 空200か）
- （参考・バックログ修正不要と判定）両repo mainのEOL(CRLF)由来未コミット差分の扱い。バックログ内容と無関係な環境衛生の話でDEV裁量/運用手順の範疇。

**傾向1（既存チェックリスト項目の3回目以降の再発）＝「先例規約未定義」**: #2の検索APIパス設計（`/api/products/search`採用）は、Sprint5 #24（i18nライブラリ/キー構造）・Sprint6 #1（ページ採番）に続く3回目の同型再発。初のドメイン機能/土台Storyに限らず、通常のfeature Story（検索機能）でもAPI命名という「後続が踏襲する先例規約」がAC未定義のまま計画フェーズの確認に持ち込まれることが確認できた。既存チェックリスト項目（下記）は既に正式昇格済みのため再昇格は不要。

**傾向2（新規パターン・初出）＝「検索/フィルタ系featureのUI配置・検索対象範囲・異常系レスポンスが未定義」**: #2のカテゴリフィルタUI配置・検索対象列（何を検索語のマッチ対象にするか）・不明パラメータ時のHTTPステータスの3件は、いずれも「検索/フィルタ系のfeature StoryがACに機能のWHAT（対象列）は書いても、UIの詳細配置と異常系/境界値のレスポンス仕様までは書ききれていない」という共通の切り口で発生した。初出のため正式昇格は見送り、次回以降、検索/フィルタ系featureで同種の質問（対象列・フィルタ配置・不正入力時のHTTPステータス）が再発した場合に正式昇格を判断する。

**傾向3（新規パターン・初出）＝「specがPOへ確定を委譲した論点の計画フェーズ確認」**: #2のLIKEメタ文字ハードニング判断は、spec文書自体が確定をPO判断に委譲していたケース。Sprint6傾向3（architecture-conventions §3.1が区分値のm_code方針をPO判断に委譲）と構造は類似するが、委譲元の文書（spec vs architecture-conventions）が異なるため厳密な2回目とはみなさず初出として記録する。次回以降、設計文書・spec側が確定をPOに委譲する構造そのものが繰り返し確認を要する場合、当該文書のfirmup（一般方針の明文化）要否を判断する。

**判定**: 傾向1は3回目以降の再発のため、既存チェックリスト項目の実例として記録に留める（正式昇格は完了済み）。傾向2・傾向3は初出のため、次回以降の再発時に正式昇格を判断する。

### Sprint 8（2026-08-16）— E2系（#4 カート追加/数量更新/削除/表示）計画フェーズより

DEVからSprint8計画フェーズでユーザーに直接確認した事項をSM経由で中継され、3件を質問ログに記録した（3件ともバックログ修正候補。うち1件はIssue #4自体ではなくarchitecture-conventions側の観点）。Epic E2（カート）はSprint8で着手・3-repo cross-repo。

- D1: 未ログイン（匿名）カートの在庫上限検証（AC-neg1）を、qty非露出（ID-28）を保ったままどうサーバに問うか→公開の冪等`GET /api/items/{itemId}/orderable?quantity=N`を新設
- D2: カート画面のルート保護境界（`meta.requiresAuth`の要否）→公開ルート、認証必須は`/api/cart/**` APIのみ
- D3: `t_cart_item`へのversion列付与要否（backend-conventions §4.3の適用是非）→付与しない（単一所有者カートは§4.3の多編集者後勝ち防止の対象外という意図的逸脱）

**傾向1（新規パターン・初出）＝「先行スプリントの意図的仕様決定（intended-diff-ledger）が後続StoryのAPI設計に及ぼす制約の未参照」**: D1は、Sprint6で確定したID-28（qty非露出）という既存の意図的仕様決定が、Issue #4起票（2026-08-11、ID-28確定より前）後に定義されたAC-neg1の実現方式と交差し、計画フェーズでの確認となった。Issue #4起票時点ではID-28が未確定だったため先回り防止は原理的に不可能だったが、確定済みの今は他Story（#9等の在庫関連Story）が同様の交差を起こしうる。初出のため正式昇格は見送るが、「Refinement/計画フェーズで対象Storyに関連するintended-diff-ledgerの既存エントリを確認し、新機能設計への制約を洗い出す」運用の必要性として記録する。

**傾向2（Sprint7傾向2の2回目の発生・適用範囲拡張）＝「新ドメインfeature全般のUI配置・保護境界未定義」**: D2（カート画面のルート保護境界）は、Sprint7 #2で観測した「検索/フィルタ系featureのUI配置・検索対象範囲・異常系レスポンス未定義」の再発。Sprint7は検索/フィルタ系に限定した記述だったが、Sprint8はカート（状態変更を伴う新ドメイン）でも同型の確認（画面の認証保護要否）が発生したため、適用範囲を「新ドメインfeature全般」に拡張して正式昇格する。

**傾向3（Sprint6傾向3の2回目の発生）＝「architecture-conventionsの一般規約がPO判断に委譲される箇所の未確定」**: D3（`t_cart_item`のversion列要否）は、Sprint6 #1で観測した「architecture-conventions §3.1（区分値のm_code登録）がPO/仕様確定を要求し、確認が発生した」パターンの、別セクション（§4.3・更新系エンティティのversion列）での再発。Issue #4自体のAC粒度の問題ではなくarchitecture-conventions文書側の設計であるため、Issue反映ではなく「architecture-conventionsのPO判断委譲箇所を起票/Refinement時に先回り確認する」観点として正式昇格する（Sprint7傾向3〔specがPOへ確定を委譲〕とは委譲元文書が異なるため別系統として扱う）。

**判定**: 傾向2・傾向3は2回目の発生条件を満たすため、下記チェックリストへ正式昇格する。傾向1は初出のため、次回以降の再発時に正式昇格を判断する。

### Sprint 9（2026-08-16）— E2系（#5 価格サーバ権威・#6 カートCSRF/冪等）計画フェーズより

DEVからSprint9計画フェーズでユーザーに直接確認した事項をSM経由で中継され、3件を質問ログに記録した（3件ともバックログ修正不要と判定＝実装アーキテクチャ判断）。加えて、SM主導のAskUserQuestionで確定した2件（Origin/SameSite検証の要否・update/mergeの数量境界値の扱い）も同系統の実装レベル論点として判定材料に含めた。#5・#6は既存feature（#4カート）への後乗せ的security hardening Story（Sprint4型＝既達判定の比重が大きい）。

- Q1: AC2「非数値quantity→400」実現のための`HttpMessageNotReadableException`→400グローバル例外ハンドラ新設要否（#6）
- Q2: XSRF-TOKEN CookieのSecure/SameSite設定方式（既存`jwt.cookie.*`再利用 vs ハードコード）（#6）
- Q3: 数量バリデーションのDTO層/サービス層非対称配置（update=DTO、merge=service）（#6）
- （参考・SM確定）#6 Origin/SameSite検証はSameSite＋トークンで充足、新規フィルタ不要
- （参考・SM確定）#5 updateの数量0は削除維持・merge数量≤0は400化

**傾向1（新規パターン・3件同一Sprint内・初出）＝「security hardening Storyにおける否定AC実装レベル論点」**: Q1〜Q3はいずれも「否定AC（非数値→400・不正入力→400等）をどう実現するか」という実装アーキテクチャ判断であり、個別にはAC先回り明記になじまない（PO/ユーザーが事前に確定値を書けるものではなく、コードベースの制約〔Bean Validation順序、Cookie設定の既存パターン、例外ハンドラの影響範囲〕を踏まえた実装時判断）と判定した。一方、3件が同一Sprint・同一Story群（#5/#6のsecurity hardening）で連続発生した点は、Sprint1(#22)が5件の下位論点を1テーマに束ねながらも初出として昇格見送りとなった前例と同型であり、1Sprint内での複数発生だけでは正式昇格の閾値（2回以上＝異なるレビュー時点での再現）を満たさないと判断する。

**（参考）Origin/SameSite確定の位置づけ**: SM確定の「Origin/SameSite検証は既存機構（SameSite＋トークン）で充足・新規フィルタ不要」は、Sprint4で正式昇格済みの「セキュリティ対策の新規追加要否は既存機構の充足可能性を先に確認する」チェックリスト項目の再発例（Sprint4 #20/#21に続く3件目）であり、追加の昇格判断は不要。

**判定**: 傾向1は初出のため、正式チェックリストへの昇格は見送り、傾向記録に留める。次回以降、既存feature/APIへの後乗せ的なsecurity hardening Storyで同種の質問（否定ACの実装方式＝エラー正規化ハンドラ・Cookie属性設定・検証層配置など）が再発した場合に正式昇格を判断する。Origin/SameSite確定は既存チェックリスト項目の再発例として扱う（判定は上記参照）。

### Sprint 10（2026-08-16）— #7 [E3] チェックアウト・ウィザード（F3.1）計画フェーズより

SM経由でSM確定3件・DEV確定4件（計7件、いずれもユーザー承認済み）を中継され、7件を質問ログに記録した（全件バックログ修正候補）。#7はチェックアウト・ウィザードの確定前段（送信・在庫引当・永続化は#8）。

- SM確定: #7 AC1のプリフィル住所データ源（E4未着手で上流提供者が不在のため、backendに`GET /api/account/me`をcross-repo新設）
- SM確定: #7/#8のスコープ境界（確定前段=#7／送信・在庫引当・永続化=#8）
- SM確定: ウィザード下書き状態の保持方式（legacy session `workingOrderForm`永続 → 新Pinia揮発）
- DEV確定: #7でorder送信APIを作るか（作らない。スコープ境界の帰結のため質問傾向としては二重計上せず）
- DEV確定: プリフィル住所APIの命名・パス（`GET /api/account/me`）
- DEV確定: 空カートで注文フォームへ進入した際の誘導UX（`/cart?reason=empty-checkout`）
- DEV確定: ウィザードのルート構成（`/checkout`単一ルート）

**傾向1（Sprint5傾向2の2回目の発生・適用範囲拡張）＝「機能Storyが依存する上流Feature未着手によるcross-repo追加」**: #7のAC1プリフィル住所データ源は、Sprint5 #24（認証状態再水和のためbackendに`GET /api/auth/me`をcross-repo新設）と同型。Sprint5では「フロント土台Story」限定の切り口として初出・昇格見送りだったが、今回は「土台Storyではなく機能Story（チェックアウト）が、未着手の上流Feature（E4アカウント管理）に依存するデータをプリフィルする」というケースで発生し、2回目の発生条件を満たした。適用範囲を「フロント土台Story」から「機能Story一般（土台Story限定ではない）」へ拡張して正式昇格する。

**傾向2（Sprint7傾向2／Sprint8で正式昇格済み項目の3回目の再発・記述強化）＝「新ドメインfeature全般の異常系UI遷移未定義」**: #7の空カート誘導UX（AC-neg1）は、Sprint7 #2で観測されSprint8 #4で正式昇格済みの「新ドメインfeature全般のUI配置・保護境界」チェックリスト項目の3回目の再発。ただし今回のトピックは「UI配置」でも「ルーティング保護境界」でもなく、前提状態が満たされない場合の異常系UI遷移（空カート状態でチェックアウトへ進入した際の遷移先・エラー表示）という、既存チェックリスト項目の文言がまだ明示的にカバーしていない側面だった。項目自体は昇格済みのため再昇格はせず、異常系UI遷移を明記する形で記述を強化する。

**（参考）ウィザード下書き保持方式・ルート構成の位置づけ**: SM確定のウィザード下書き保持方式（Pinia揮発）とDEV確定のルート構成（`/checkout`単一ルート）は密結合の1テーマ（下書き揮発のためdeep-link非対応にした）。新規パターンとしては初出のため傾向記録に留め、次回以降、多段ウィザード系Storyで同種の質問が再発した場合に正式昇格を判断する。挙動差そのものは`spec/intended-diff-ledger.md`にID-30として追記済み（legacy `workingOrderForm`のsession永続 → 新Piniaの揮発。ID-19〔未ログインカートのクライアント保持〕と同様の「観測可能な挙動差」に該当すると判定）。

**判定**: 傾向1・傾向2はいずれも2回目以降の発生条件を満たすため、傾向1は正式昇格・傾向2は既存項目の記述強化を行う。ウィザード下書き保持方式は初出のため、次回以降の再発時に正式昇格を判断する。

### Sprint 11（2026-08-17）— E3系（#8 注文確定・サーバ再計算・在庫原子的引当・整合性）計画フェーズより

SM経由でSM/DEV確定7件（いずれもユーザー承認済み）を中継され、7件を質問ログに記録した（5件がバックログ修正候補・2件はバックログ修正不要と判定＝既存台帳の適用確認1件・実装アーキテクチャ判断1件）。#8はチェックアウト・ウィザードの確定後段（送信・在庫引当・永続化。確定前段=#7）。

- 在庫不足・同時発注競合負け（ガード付きUPDATE affected rows==0）による注文失敗のHTTPステータス→409 Conflict（既存の楽観ロック競合と系統統一）
- 空カートでの確定リクエスト到達時の応答→409 Conflict（在庫不足と同じ系統）
- #8の注文完了画面のスコープ→最小（注文番号＋合計＋サンクスのみ）。明細一覧は#10へ委譲
- 確定時のマスター価格変動時の挙動→再計算値でそのまま確定（差分検知なし）
- 注文失敗の監査ログ記録要否（AC6「結果」の解釈）→成功・失敗の両方を記録（失敗はREQUIRES_NEW別txでresult=FAILURE）
- （参考・バックログ修正不要）支払プレースホルダ／email・phoneの扱い→既存台帳ID-8/ID-21の適用確認
- （参考・バックログ修正不要）在庫減算行のWHO付与方式→単一POJO引数＋Interceptor自動付与（MyBatis実装制約に基づくDEV裁量）

**傾向1（Sprint9傾向1の条件拡張・2回目の発生）＝「異常系/否定ACのHTTPステータス未定義」**: #8の在庫競合・空カート確定失敗時のHTTPステータス（409）確認は、Sprint9 #6のQ1（非数値quantity→400ハンドラ新設要否）と同根の「否定AC/異常系ACが失敗することは書かれていても、対応するHTTPステータスコードがAC文言に定量的に書かれていない」というギャップに起因する。Sprint9時点の条件は「既存feature/APIへの後乗せ的なsecurity hardening Story」に限定していたが、#8は新規feature Story本体（既存機能への後乗せではない）で発生しており、条件を厳密には満たさない。一方、Sprint5傾向2→Sprint10傾向1、Sprint7傾向2→Sprint8傾向2、Sprint6傾向3→Sprint8傾向3で採用してきた「異なる文脈での再発時は条件を拡張して正式昇格する」運用方針に倣い、条件を「security hardening Story限定」から「異常系/否定ACを含むfeature Story全般」へ拡張して正式昇格する。

**傾向2（新規パターン・初出）＝「監査ログAC文言の対象範囲（成功/失敗の粒度）未定義」**: AC6「結果」の解釈確認は、Sprint2（監査ログの記録範囲＝WHO行レベル/eventレベルの責務分離）・Sprint4（#21 AC2＝既存機構recordAuthzFailureで足りるか）のいずれとも異なる切り口（記録"対象"が成功のみか失敗も含むかという解釈の曖昧さ）のため、初出として記録する。次回以降、監査ログを扱うStoryで同種の質問（成功/失敗・部分成功等の粒度確認）が再発した場合に正式昇格を判断する。

**傾向3（Sprint8傾向1の2回目の発生）＝「intended-diff-ledgerの既存エントリが後続StoryのAPI/データ設計に及ぼす制約の確認」**: 支払プレースホルダ／email・phoneの扱い確認は、Sprint8 #4のD1（ID-28がAC-neg1実現方式に及ぼす制約の確認）と同型の「計画フェーズで対象Storyに関連するintended-diff-ledgerの既存エントリを確認する」動きの2回目の発生（今回は新規設計ではなく既存エントリ〔ID-8・ID-21〕で充足済みという確認結果だった点は異なるが、"ledgerとの整合を計画フェーズで確認する"という運用自体は同型）。Sprint8時点では「運用の必要性」として記録に留めていたが、2回目の発生条件を満たすため正式昇格する。

**判定**: 傾向1・傾向3は2回目の発生条件を満たすため、下記チェックリストへ正式昇格する（傾向1は条件拡張・傾向3はSprint8時点の運用記録を正式チェックリスト項目化）。傾向2は初出のため、次回以降の再発時に正式昇格を判断する。

### Sprint 12（2026-08-17）— E6系（#28〜30 Repository層リファクタ）計画フェーズより

2026-08-17の#28〜30 Refinement（対ユーザー、PO主導のスコープ是正）と、Sprint12計画フェーズでDEVからユーザーに直接確認された設計論点2件（D1集約の型戦略・D2 Repository書込メソッド粒度、SM経由で中継）を棚卸しした。#29はdomain層Repository interfaceの初回PoC（Issue本文に「#30が踏襲する先例規約」と明記されたStory）。

- Refinementでの気付き: #30起票時点のスコープ（対象Application Service）は、実コード裏取り前はOrderの違反漏れ・Authの誤含めがあり不正確だった。実コード確認後に是正。
- D1: 集約の型戦略（単一Cart/CartItem型のrich化 vs 書込集約/読取read-modelの物理分離＝完全CQRS）
- D2: Repository書込メソッド面の粒度（細粒度ドメイン語彙メソッド vs 集約全体差分save(Cart)）

**傾向1（既存チェックリスト項目の3件目の再発）＝「先例規約未定義」**: D1・D2は、Sprint5 #24（i18nライブラリ/キー構造）・Sprint6 #1（ページ採番）で正式昇格済みの「初のドメイン機能/土台Storyが後続Story群の再利用する先例規約を確立する場合、その確定値をAC/備考に先回り明記できないか確認する」チェックリスト項目の3件目の再発。#29のAC1は「（#30が踏襲する先例規約）」と論点の存在を明記しつつ、集約の型戦略・書込メソッド粒度という具体的な確定値までは書き切れていなかった。既存チェックリスト項目に該当するため新規昇格は不要（下記チェックリストの引用リストへ追記）。

**傾向2（新規パターン・初出）＝「リファクタ/是正Story起票時のスコープが実コード未裏取りでずれる」**: #30の当初スコープ（対象Application Service）は、Issue起票時点でOrderの違反が漏れ・Authが誤って含まれるなど、実際のドリフト実態と食い違っていた。2026-08-17のRefinementで実コード（grep等）を確認したうえで是正した。これまでのfeature Story起票（望ましい新規挙動を書く）とは異なり、リファクタ/棚卸し系Story（既存コードの実態是正が目的）は起票時点でスコープの正確性が実コードの現状把握に依存するという性質がある。初出のため正式昇格は見送り、傾向記録に留める。次回以降、既存コードの是正を目的とするリファクタ/棚卸し系Story起票時に同種の指摘（起票時のスコープ記述が実装実態と食い違っていた）が再発した場合、「リファクタ/棚卸し系Storyの起票前に対象範囲をgrep等の実コード確認で裏取りする」観点としてチェックリストへの正式昇格を判断する。

**判定**: 傾向1は3件目の再発のため、既存チェックリスト項目の実例として引用リストに追記する（正式昇格は完了済みのため再昇格は不要）。傾向2は初出のため、次回以降の同種発生時に正式昇格を判断する。

### Sprint 13（2026-08-17）— E6系（#30 Repository層全展開）計画フェーズより

DEVからSprint13計画フェーズでユーザーに直接確認した設計論点3件（Q1 Order集約 vs orchestrationの線引き・Q2 read/write Repository命名統一・Q3 InventoryRepositoryのパッケージ配置）をSM経由で中継され、3件を質問ログに記録した（3件ともバックログ修正候補）。#30は#29で確立したテンプレをCatalog/Account/Orderへ横展開するStory。

- Q1: backend-conventions §9「書き込み側は集約リポジトリにする」をOrderにも適用し、rich な Order 集約を新設するか、#8の並行オーケストレーション（原子引当・item_id固定順・all-or-nothing・監査）をApplicationに残すかの確認。Issue #30の方針欄「Order は #29 の集約パターンに準拠する」はrich集約化を示唆するように読めたが、実際は案A（rich集約は作らずorchestrationを残す）で確定した。
- Q2: read/write Repositoryの命名を分けるか（DEV推奨のread=`...Query`／write=`...Repository`分離）の確認。統一`...Repository`命名採用（ユーザーオーバーライド）。
- Q3: `InventoryRepository`のパッケージ配置の確認。新規`domain/inventory`（`domain/cart`と並行構造）で確定。

**傾向1（既存チェックリスト項目「architecture-conventionsのPO判断委譲パターン」の3件目の再発）**: Q1は、Sprint6 #1（§3.1）・Sprint8 #4（§4.3）に続き、backend-conventions §9という一般規則がOrder固有の既存複雑オーケストレーション（#8）への適用深度までは規定しておらず、PO判断が必要になった事例。既存チェックリスト項目に該当するため新規昇格は不要（引用リストへ追記）。

**傾向2（既存チェックリスト項目「先例規約未定義」の4件目・5件目の再発）**: Q2・Q3は、Sprint5 #24（i18nライブラリ/キー構造）・Sprint6 #1（ページ採番）・Sprint12 #29（集約の型戦略・save粒度）に続き、後続Story群が踏襲する先例規約（Repository命名・パッケージ構造）の確定値がAC/方針欄に書き切れていなかった事例。Q2はIssue #30自身が方針欄で「Repository / Query インターフェイス」と書き分けを示唆する表現を使っていたにもかかわらず確定値が未記載だった点が特徴的。既存チェックリスト項目に該当するため新規昇格は不要（引用リストへ追記）。

**付記（傾向としては非昇格）**: Q2はDEV推奨（read=Query/write=Repository分離）をユーザーが統一Repository命名にオーバーライドした事例。Sprint5 #24・Sprint6 #1でも同様のユーザーオーバーライドが発生しているが、DEVの技術提案の当否自体はAC先回り明記になじまない実装時判断の性質が強いため、「オーバーライド」自体は独立の傾向として追跡しない（命名規約という結果面は傾向2に計上済み）。

**判定**: 傾向1・傾向2はいずれも既存チェックリスト項目の再発のため新規昇格は不要。引用リストへ〔Sprint13 #30〕を追記する。

### Sprint 14（2026-08-17）— E3系（#9 注文履歴一覧・#10 注文詳細閲覧）計画フェーズより

DEVからSprint14計画フェーズでユーザーに直接確認した事項をSM経由で中継され、1件を質問ログに記録した（バックログ修正候補・かつ`rules/database.md`の文書ギャップ是正候補）。#9はページング用複合索引`(user_id, order_id)`を`t_order`に新設するにあたり、既存単一列索引`idx_t_order_user_id (user_id)`が左端プレフィックスで完全内包され冗長化する問題が発生した。

- #9: ページング用複合索引`(user_id, order_id)`追加時、既存単一索引`idx_t_order_user_id`を「置換（ADD+DROP）」するか「両方残す」か→置換を採用（両方残すと重複索引でperfレビュー指摘対象。FKは複合索引の左端でバックされ継続するためDROPは安全）

**傾向（PO長期記憶では初出だが、SM長期記憶に記録済みのSprint1トレンドと同一文書ギャップの2件目）＝「索引設計方針が`rules/database.md`に未文書化」**: 本件はPO自身の質問ログとしては初出だが、SM長期記憶（`memory/sm/long_term.md:48`）が記録するSprint1「DBスキーマStory（#22）でのFK列明示セカンダリインデックスの一貫性確認・2回目の発生でrules/database.mdへの昇格を判定」というトレンドと、切り口（Sprint1=FK列に明示索引をADDすべきか／Sprint14=複合索引導入時に既存単一索引をDROPして重複排除すべきか）は逆だが、いずれも「`rules/database.md`に索引設計方針（FK明示索引の付与基準・複合索引と単一索引の重複排除基準）が存在しない」という同一の文書ギャップに起因する。PO長期記憶で採用してきた「同一ギャップに起因する別切り口での再発は条件拡張のうえ正式昇格する」運用（Sprint5→10のcross-repo追加、Sprint9→11のHTTPステータス未定義）に照らし、本件をSprint1トレンドの実質2件目（索引ハイジーン方針の欠如という上位パターンでの再発）と判定した。

**判定**: 「先例規約未定義」等の既存PO内チェックリスト項目とは異なり、本件は個別StoryのAC先回り明記では解決しない**一般的なDBエンジニアリング方針**（FK索引の付与基準・複合索引による単一索引の置換基準は、Story文脈に依存せず一度定義すれば以降のStoryに一律適用できる）と判断し、PO内チェックリストへの項目追加ではなく、`rules/database.md`に**索引ハイジーン節を新設して直接firmupする**方針を採用した。`.claude\rules\database.md`に「索引ハイジーン（FK 明示索引・複合索引導入時の重複排除）」節を追加済み（本Retroで実施）。今後のDB関連Story（#10含む今後のE3・E4）ではAC先回り明記ではなく本ルール参照で足りるため、PO先回りチェックリストへの追加は行わない。`#skills-changelog`へ[PO]で投稿する。

### Sprint 15（2026-08-17）— E3系（#11 remoting/WS getOrder廃止・#12 支払プレースホルダ化）・refactor（#28 カートマージバッチ化）計画フェーズより

SM経由でSprint15計画フェーズの確認・確定事項5件（SMのspec委譲論点先回りとDEVの実コード精読を通じたもの）を中継され、5件を質問ログに記録した（全件バックログ修正候補）。#11/#12は既存決定・既存台帳（ID-5・ID-8）との整合確認が中心のE3継続後乗せStory、#28はカートマージのパフォーマンス改善リファクタ（挙動不変が絶対要件）。

- #11: remoting/WS getOrder廃止（security）が構造的に既達（sink不在等）と判明した場合の成果物定義→回帰テスト＋明文化で固定
- #12: 支払プレースホルダ文言（現行「将来追加」ニュアンス）がID-8の意図（カード情報を扱わない/保持しない）と食い違っていないか→「扱わない」明示に変更
- #12: 未参照の孤立カード型定義（frontend enum/定数）の撤去範囲→孤立enum/定数のみ撤去、DBのm_code 0002は温存（cross-repo回避）
- #12: backend `CardType.java`削除の永続性→削除しない。m_code 0002からのcodegen生成物と判明し、削除してもgenerateEnums手動実行で再生成されるため。frontend定数のみ撤去
- #28: カートマージのバッチ化で重複itemId入力時の挙動→coalesceで挙動不変を厳守。バックログ前提「localStorageはitemIdキーmap」をDEVが実コードで訂正（実体は配列・dedupは書込ロジック側が担保）

**傾向1（既存チェックリスト項目の4件目の再発）＝「secure-by-default機構の既存充足確認」**: #11の成果物定義確認は、Sprint4 #20(SBD-9)/#21(AC2)・Sprint9 #6(Origin/SameSite)に続く4件目の再発。「既存実装で構造的に解消済み・新規実装は不要、回帰テスト＋明文化で固定」という結論もSprint4 SBD-9型と完全に一致。既存チェックリスト項目に該当するため新規昇格は不要（引用リストへ追記）。

**傾向2（既存チェックリスト項目「リファクタ/是正Story起票時のスコープが実コード未裏取りでずれる」〔Sprint12傾向2〕の2回目以降の発生・適用範囲拡張）＝「起票/計画時の前提〔スコープ・データ構造・削除対象の生成物由来〕の実コード裏取り不足」**: #28（バックログ前提「localStorageはitemIdキーmap」が実コード〔配列〕と食い違っていた）は、Sprint12 #30（対象Application Serviceの範囲ずれ）と同一角度（起票/バックログ記述時点の記述が実装実態と食い違う）の2回目の発生であり、単独でも正式昇格条件（2回以上）を満たす。加えて#12のCardType.java確認（撤去対象が手書きコードかcodegen生成物かという前提が未確認だった）も、切り口は異なるが同じ上位パターン（実コード未裏取りによる前提の誤り）に属する3件目の実例。これを機に、チェックリスト項目の対象を「対象範囲（スコープ）」に限定せず「前提（データ構造・削除対象の生成物由来等）」まで含む形に拡張して正式昇格する。

**傾向3（新規パターン・初出）＝「intended-diff-ledgerとの整合確認対象をUI表示文言のニュアンスへ拡張できないか」**: #12支払文言確認は、Sprint8 #4・Sprint11 #8で正式昇格済みの「Storyの起票/Refinement時にintended-diff-ledgerの既存エントリを確認し、新機能の設計・データ項目が制約範囲内かAC/備考に先回り明記できないか確認する」チェックリスト項目と同根の動きだが、対象が「設計・データ項目の制約」ではなく「UI表示文言のニュアンスがledger決定（ID-8）の意図と整合しているか」という初出の切り口。今回が初出のため、正式なチェックリスト項目の適用範囲拡張は見送り、傾向記録に留める。次回以降、同種（既存ledger決定とUI表示文言のニュアンスの不整合）が発生した場合に適用範囲拡張を判断する。なお本件はチェックリスト昇格とは別に、`spec/intended-diff-ledger.md`のID-8記述強化（文言が「扱わない/保持しない」旨を明示する点・孤立型残骸の撤去範囲を追記）で対応済み（Sprint11 RetroのID-22強化と同型の運用）。

**（参考）孤立型撤去範囲の位置づけ**: #12の孤立カード型定義の撤去範囲（enum/定数のみ撤去・DB m_code 0002は温存）は、CardType.javaのcodegen生成物確認と一体の決定であり、独立の傾向としては追跡しない（結果は傾向2・ID-8強化に計上済み）。

**判定**: 傾向1は4件目の再発のため引用リストへ追記のみ（新規昇格不要）。傾向2は2回以上（実質3件）の発生条件を満たすため、既存チェックリスト項目の説明を拡張して正式昇格する。傾向3は初出のため、正式昇格は見送り傾向記録に留める（ledger側の強化のみ実施済み）。

### Sprint 16（2026-08-17）— E4系（#13 登録・#14 アカウント編集）計画フェーズより

DEVからSprint16計画フェーズでユーザーに直接確認した事項9件（E1〜E9）と、SMが先回りでユーザー承認取得した3件（メール検証プレースホルダ／入力検証委譲／PW変更委譲）、計12件をSM経由で中継され、質問ログに記録した（9件がバックログ修正候補・3件は既存台帳/チェックリストの適用確認でバックログ修正不要）。E4（アカウント）はSprint16でE4初のwriteドメインとして着手（#13登録・#14編集）。

- E1: 登録レート制限（AC-neg2/SBD-6）の保持先はin-memoryかDB-backedか→DB-backed採用（`t_register_attempt`、既存`t_login_attempt`と同方式）
- E2: account/profile両表のversion扱いは単一トークンか二重トークンか→`m_account.version`単一トークン採用
- E3: 編集用readは既存`GET /api/account/me`拡張か新設か→新規`GET /api/account`（version込み）
- E4: username重複時のレスポンス（409+明示メッセージ）は列挙対策(AC4)と整合するか→整合（列挙対策はレート制限が担保）
- E5: 登録フォームにlangpref/favcategory入力欄を出すか→出さない（DTO optional受理のみ）
- E6: `.jps-required`日本語ラベルの扱い→英語化（既存不整合の是正・Sprint16バックログR4で想定済み）
- E7: 登録WHOのcreate_user_id（未認証guestアクター）の値→NULL（architecture-conventions §2.1準拠）
- E8: 登録エンドポイントのパス・permitAll設計→`POST /api/register`独立パス
- E9: 自動ログインのJWT発行処理の抽出配置→`issueTokensFor`共通化（実装アーキテクチャ判断・DEV裁量）
- SM先回り確定3件: メール検証プレースホルダ（ID-8型）／入力検証はas-is同等のみ実装しID-16どおり#17/#15へ委譲／PW変更は#15へ委譲。いずれも既存台帳（ID-8型・ID-16・ID-13）またはスコープ境界チェックリストの適用確認で、DEVへの再質問なしにSM単独で計画フェーズ解決できた成功例。

**傾向1（既存チェックリスト項目のどれにも完全一致しない中間的性質・個別分析の結果アーキ規約側でfirmup）＝「secure-by-defaultカウンタの永続化方式（DB vs in-memory）」**: E1は、Sprint4 #20（ログインロックアウトのDB表保持）と同一設計軸の、12スプリントを隔てた再発。POチェックリストのどの既存項目にも完全一致しないため、Sprint16質問中継時点では「次回Retroで判定」として保留していた。**本Retroで判定：PO内チェックリストへの新規昇格ではなく、Sprint14の「DB索引ハイジーン」節新設（`rules/database.md`直接firmup）と同型の対応として、`spec/architecture-conventions.md`にD7（secure-by-default系カウンタの永続化＝DB-backed）を新設・直接firmupした**（本Retroで実施。テーブル設計・キー戦略・閾値等の実装詳細はbackend-conventions/DEV裁量に委ね、spec側は原則のみを明文化）。

**傾向2（既存チェックリスト項目「architecture-conventionsのPO判断委譲パターン」の4件目の再発）**: E2（version単一/二重トークン）は、§3.1〔Sprint6〕→§4.3〔Sprint8〕→§9〔Sprint13〕に続く§4.2の4件目の再発。既存チェックリスト項目に該当するため新規昇格は不要（引用リストへ追記）。

**傾向3（既存チェックリスト項目「先例規約未定義」の再発）**: E3（編集用readエンドポイント新設/既存拡張の判断）・E8（登録エンドポイントのパス設計）は、Sprint5 #24／Sprint6 #1／Sprint7 #2／Sprint12 #29／Sprint13 #30に続く事例。E7（WHO create_user_idのguestアクター値）はarchitecture-conventions §2.1が答えを持っていたが備考未引用だった点で同型の一種。いずれも新規昇格不要。

**傾向4（既存チェックリスト項目「secure-by-default機構の既存充足確認」の5件目の再発）**: E4（username重複409と列挙対策の整合）は、Sprint4 #20/#21・Sprint9 #6・Sprint15 #11に続く5件目の再発。新規昇格不要。

**傾向5（新規パターン・初出。観察継続）＝「DTO optionalフィールドの登録画面UI表示要否が未定義」**: E5は初出のため正式昇格は見送り、次回以降の同種発生時に判断する。

**（参考）E6・E9・SM先回り確定3件の位置づけ**: E6はSprint16バックログ自身が「留意（既存不整合）」R4として既知リスク記録済みのため確認完了として扱う。E9は実装アーキテクチャ判断（DEV裁量）。SM先回り確定3件は既存チェックリスト運用が機能している証跡として記録する。

**判定**: 傾向1は本Retroでarchitecture-conventions D7として直接firmup（PO内チェックリストへの昇格ではない）。傾向2〜4は既存チェックリスト項目の再発のため引用リストへ追記のみ（新規昇格不要）。傾向5は初出のため観察継続。

**（本Retro追加判定・team-lead依頼分）**:

- **メール検証プレースホルダ（#13 Q1）の別バックログ化**: `mcp__github__list_issues`（state:open）で重複が無いことを確認したうえで、新規Issue **#32**「[E4] 登録時のメールアドレス所有権を検証する（メール確認リンク）」をReady=NotReady（deferred）で起票した（Sprint15 #12支払プレースホルダの持ち越し運用と同型）。
- **intended-diff-ledgerへの反映**: #13のsecure-by-default差分のうち、**登録レート制限**はSBD-6の同一設計軸としてID-11を拡張した（登録試行=`t_register_attempt`・関連Storyへ#13を追加、legacyには登録試行への列挙対策自体が存在しなかった旨を明記）。一方、**メール検証プレースホルダ**は、legacy側も元々メール検証を持たず（`spec/behavior/account.md` S12/R14はspecが新規に提案した対策であり、legacy実装ではない）、実装を見送った現時点では観測可能な旧新差分が生じていないため、**新規IDは不要**と判定した（ID-8〔支払プレースホルダ〕は実際にカード列/入力欄を撤去する実装差分が生じたが、本件は実装自体を行っていない点が異なる）。#32着手時に改めてID追記要否を判定する。ID-2（ハッシュ）／ID-7（bannerdata）／ID-10（セッション再生成）は、いずれも既に関連Storyへ#13が登録済みで対応済みと確認した。

### Sprint 17（2026-08-18）— E4系（#15 パスワード変更再認証・#16 CSRF・#17 入力検証）計画フェーズより

DEVからSprint17計画フェーズでユーザーに直接確認した事項をSM経由で中継され、1件を質問ログに記録した（バックログ修正候補）。E4（アカウント）のセキュリティ完結スプリント（#15 PW変更再認証・#16 CSRF回帰・#17 入力検証）で、E4はSprint17完了をもって機能面完了予定。

- #15: パスワード変更AC-neg1「現在パスワード誤り時のレスポンス（401/403相当）」の確定値→422（新設`InvalidCurrentPasswordException`）。1エンドポイント内の4失敗系統（現在PW誤り／弱PW・不正入力／真の未認証／CSRF欠落）をそれぞれ422/400/401/403へ分離。

**傾向1（Sprint16→17中継時点〔2026-08-18②c〕で初出と判定・本Retroで2回ルール再判定）＝「1エンドポイント内の複数エラー系統のステータス配分＋既存クロスカット処理との衝突確認」**: 中継時点では、既存チェックリスト項目「異常系/否定ACのHTTPステータス未定義」（Sprint9→Sprint11で正式昇格済み）の3件目の再発とは別に、「新規定義するコードが既存のクロスカット処理（httpClientのsilent-refresh割込み=401／CSRFミドルウェア=403）と意味衝突しないか確認する」という切り口が初出と判定され、再発候補として今スプリント残りのE4 Story（#16 CSRF・#17入力検証）が挙げられていた。本Retroで実装結果（`backlog/sprint_17/sprint_backlog.md`のQ4・#16/#17備考）を確認したところ、**#16はCSRF対策の既達確認（Q4「既達を回帰+明文化」）で単一失敗理由（CSRFトークン欠落=403）のみ、#17は検証エラーをbackend権威400で一律統一する既定を維持（field-levelへの拡張なし）で単一失敗理由（400）のみと、いずれも1エンドポイント内で複数失敗理由が同居する構造ではなく、当該切り口は再発しなかった**と判定する。2回ルールの発生条件（異なるレビュー時点での再現）を満たさないため、**正式昇格は見送り、傾向記録を継続する**（次回以降、複数失敗理由が1エンドポイントに同居する状態変更APIを扱うStoryが出た場合に再判定）。

**判定**: 傾向1は再発なしのため正式昇格を見送る（傾向記録を維持・引き続き観察）。既存チェックリスト項目「異常系/否定ACのHTTPステータス未定義」の引用リストへ〔Sprint17 #15〕を追記する（下記チェックリスト参照）。

### Sprint 18（2026-08-18）— bug #33（保護ルート実機到達性）・#34（ナビ導線欠落）／refactor #27・#31・#26（技術的負債焼却）計画フェーズより

DEVからSprint18計画フェーズでユーザーに直接確認した事項（#34 Q1・UI配置の委譲論点）をSM経由で中継され、1件を質問ログに記録した（バックログ修正候補）。本スプリントは2026-08-18のライブ手動検証で発覚した出荷済み機能（#7/#9/#10/#14/#15）の到達性バグ2件（#33認証ガードのレース・#34ナビ導線欠落）の修正と、E4完結後に残った軽量技術的負債3件（#27 tsconfig baseUrl・#31 null警告・#26依存版currency）の焼却で構成された。SM洗い出しのQ2（#33再水和順序）・Q3（#31解消手段統一）・Q4（#26更新トリガ基準）はいずれも実装詳細寄りの委譲論点でDEV精読後に確定し、ユーザーへの再質問なしで計画フェーズ解決できた（Sprint16 SM先回り確定3件と同型の成功例）。

- Q1: #34 AC1「ヘッダー（またはアカウントメニュー）から...遷移できる」の配置形態（平置き vs ドロップダウン）両許容→案A-1（`.app-header__account`クラスタ内に平置き）で確定

**傾向1（既存チェックリスト項目「新ドメインfeature全般のUI配置・保護境界」の再発・同根だが新しい特徴を伴う）**: Q1は、Sprint7 #2・Sprint8 #4・Sprint10 #7で正式昇格済みの「UI配置の確定値がAC/備考に書かれていない」チェックリスト項目と同根（「ACがUI配置の確定値を書き切れていない」という同一ギャップ）。ただし従来3例はAC/備考がUI配置について完全に未定義（沈黙）だったのに対し、#34 AC1は「ヘッダー（またはアカウントメニュー）」と選択肢を明示的に並記し両方許容する書き方だった点が新しい特徴（未定義よりは一歩踏み込んでいたが、実装レベルの具体的選択＝平置き/ドロップダウンは依然未確定だった）。既存チェックリスト項目の対象範囲には該当するため独立の新規パターンとしての2回ルール判定は不要と判断し、既存チェックリスト項目の引用リストへ〔Sprint18 #34〕を追記するとともに、説明文に「ACが複数の配置候補を『または』で並記し両方許容と読める書き方をしている場合も対象に含む」旨の記述強化を行う（下記チェックリスト反映済み）。

**（参考）「SPA保護ルートの実機到達性」傾向の位置づけ**: 前回Refinement（2026-08-18②）で記録した「フロントSPAの実機到達性（リロード/直リンク/導線配線）がAC/受入検証の観点から漏れやすい」傾向は、その発生源自体が本スプリントの#33/#34（Sprint18で初めて着手・修正されたIssue）であるため、本Retro時点でこの傾向の「2回目の発生」はまだ観測されていない（#33/#34着手はこの傾向の初出そのものであり、着手した事実だけでは再発とみなさない）。上記のQ1（UI配置委譲）とは別トピック（Q1はAC記述の完成度の話・実機到達性傾向は検証/受入テストの見落としの話）と判定し、両者を混同しない。実機到達性傾向は正式昇格を見送り、次回以降のSPA保護ルート系Storyでの再発時に判定する（傾向記録を継続）。

**判定**: 傾向1は既存チェックリスト項目の再発のため引用リストへ追記＋記述強化（新規昇格は不要）。「SPA保護ルートの実機到達性」傾向は本Retro時点で再発なし（発生源自体が本スプリントのため）と判定し、正式昇格を見送り観察継続する。

## バックログ起票時の先回りチェックリスト

**土台Story（DB移行・backend/frontend土台・認可/認証基盤など、新規リポジトリ・新規レイヤー・新規ドメイン横断機構に着手するStory）を起票・詳細化する際は、以下をAC/備考に先回り明記できないか確認する**（Sprint1 #22・Sprint2 #23の計11件中9件がAC/備考の先回り明記で防止できたと判定され、2Sprint連続発生のため正式昇格。Sprint4 #21で「E6基盤系」に限らず認可/認証土台Storyにも適用範囲を拡張）：

- [ ] テーブル・列の命名規約（プレフィックス・大小文字規則）〔Sprint1 #22〕
- [ ] 主キー戦略（surrogate/natural key、型。WHO列などの前提整合）〔Sprint1 #22〕
- [ ] 既存テーブル・既存機能の統合/廃止方針（対象列名・対象Issueまで具体化）〔Sprint1 #22〕
- [ ] Sprint内のseed/fixtureスコープ（DDLのみか、バルクデータ投入まで含むか）〔Sprint1 #22〕
- [ ] スコープ境界（本Storyの範囲=どこまでが基盤で、どこからがドメインStoryか。除外する機能と送り先Issue番号を明記。画面単位・共通コンポーネント単位・アセット単位など、Story種別に応じた具体的な範囲区分まで踏み込む）〔Sprint2 #23／Sprint4 #21／Sprint5 #24で再発・適用範囲を認可/認証・frontend土台Storyへ拡張〕
- [ ] 未実装の依存機能がある場合の実証手段（例：ログインEP不在下でのトークン発行実証方法、保護ルート実証を純関数テストで代替可能か等）〔Sprint2 #23／Sprint5 #24〕
- [ ] 秘密情報・設定値の管理方針（デフォルト値許容かfail-fastか）〔Sprint2 #23〕
- [ ] 外部システム・他リポジトリ資産との連携方式（統合テストでの他repoスキーマ適用方法等）〔Sprint2 #23〕
- [ ] セキュリティ対策（secure-by-default機構）の新規追加要否を判断する前に、既存実装で要件を満たしている可能性を確認し、「既存機構Xで達成済み・新規実装不要」または「新規実装が必要な理由」をAC/備考に明記する〔Sprint4 #20（SBD-9）・#21（AC2）／Sprint9 #6（Origin/SameSite）／Sprint15 #11（remoting/WS getOrder廃止＝構造的既達・回帰テスト＋明文化で固定）／Sprint16 #13（E4・username重複409と列挙対策の整合）〕
- [ ] Vue3 SPA化に伴うfeature/security Storyでフロント側に波及する要素（画面文言・プリフィル値・遷移UX等）がある場合は、対象内/対象外を明示し、対象外なら持ち越し先Issue番号をAC/備考に明記する〔Sprint3 #18・Sprint4 #20〕
- [ ] AC等に定量パラメータ（閾値・回数・期間・上限など）が定性的表現のみで書かれていないか。既存踏襲値がある場合は先回り明記し、新規UX値で具体値を決め切れない場合も「計画フェーズで確定が必要」である旨と決定候補を備考に明記する〔Sprint4 #20（レート制限閾値）・Sprint6 #1（残少バッジ閾値N）〕
- [ ] 初のドメイン機能/土台Storyが、後続Story群が再利用する先例規約（採番方式・命名規則・DTO形・横断ライブラリの技術選定等）を確立する場合、その規約の確定値をAC/備考に先回り明記できないか確認する。Issueの論点として存在は認識されていても、具体的な確定値が書かれていなければ計画フェーズで確認が発生しうる〔Sprint5 #24（i18nライブラリ/キー構造）・Sprint6 #1（ページ採番）・Sprint12 #29（集約の型戦略・save粒度）・Sprint13 #30（Repository命名統一・Inventoryパッケージ配置）・Sprint16 #14（編集用readエンドポイント新設）・#13（登録エンドポイントのパス設計）〕
- [ ] 新ドメインfeature Story（検索/フィルタ系に限らず、カート・チェックアウト等の状態変更を伴うfeatureを含む）の画面は、UI配置・ルーティング保護境界（`meta.requiresAuth`の要否・未ログイン利用可否）に加え、前提状態が満たされない場合の異常系UI遷移（例：空カートで注文フォームへ進入した際の誘導先・エラー表示）までAC/備考に先回り明記できないか確認する。ACが複数の配置候補を「または」で並記し両方許容と読める書き方をしている場合も対象に含む（選択肢の提示だけでは配置形態の確定にならない）〔Sprint7 #2（UI配置・検索対象範囲・異常系レスポンス）／Sprint8 #4（カート画面のルート保護境界）／Sprint10 #7（空カート誘導UX）／Sprint18 #34（ヘッダーナビ導線の配置形態＝平置き vs ドロップダウン、ACが両許容のまま実装委譲）〕
- [ ] architecture-conventionsが一般規約を示しつつ個別確定をPO判断に委ねている項目（例: §3.1区分値のm_code採用、§4.3更新系エンティティのversion列）に該当するStoryを起票/Refinementする際は、その規約の確定値・適用可否をAC/備考に先回り明記できないか確認する〔Sprint6 #1（§3.1）／Sprint8 #4（§4.3）／Sprint13 #30（§9・Order集約深度）／Sprint16 #14（§4.2・account/profileのversion単一/二重トークン）〕
- [ ] 機能Story（土台Story限定ではない）のACが未着手の上流Feature/Epicのデータ・APIに依存する場合（例：他ドメインからのプリフィル・参照系データ取得）、必要な取得手段（cross-repoでのAPI新設要否を含む）をAC/備考に先回り明記できないか確認する〔Sprint5 #24（認証状態再水和・`GET /api/auth/me`新設）／Sprint10 #7（配送先プリフィル・`GET /api/account/me`新設）〕
- [ ] 異常系/否定AC（在庫不足・不正入力・競合等による失敗）を含むStoryは、対応するHTTPステータスコードを既存の先例パターン（例：楽観ロック競合=409、不正入力=400）に揃えるか新規定義するかをAC/備考に先回り明記できないか確認する〔Sprint9 #6（非数値quantity→400ハンドラ）／Sprint11 #8（在庫競合・空カート→409）／Sprint17 #15（PW変更エラー系統の三分離=422/400/401/403）〕
- [ ] Storyの起票/Refinement時に、対象ドメイン・データに関連する`spec/intended-diff-ledger.md`の既存エントリを確認し、新機能の設計・データ項目がその制約範囲内か（新規実装が必要か、既存決定で充足済みか）をAC/備考に先回り明記できないか確認する〔Sprint8 #4（D1・ID-28がAC-neg1に及ぼす制約）／Sprint11 #8（ID-8/ID-21で支払・email・phone扱いが充足済みと確認）〕
- [ ] リファクタ/是正・棚卸し系Story（既存コードの実態是正・撤去が目的のStory）を起票・詳細化する際は、対象範囲（スコープ）だけでなく前提となる実装の詳細（データ構造・削除対象が手書きコードかcodegen生成物か等）についても、起票前にgrep等の実コード確認で裏取りできないか確認する。裏取りが難しい場合は「計画フェーズでの実コード確認が必要」である旨をAC/備考に明記する〔Sprint12 #30（対象Application Serviceの範囲ずれ）／Sprint15 #12（CardType.javaのcodegen生成物由来未確認）／Sprint15 #28（localStorageのデータ構造前提の誤り）〕

## 過去の意思決定ログ

- 2026-08-13（Sprint2計画フェーズ、#23）: バックエンド・アーキ土台Story(#23)はドメインservice/endpointを含まない方針を確定。ドメイン機能はlogin=#21・在庫=#8・account編集=#14へ委譲する。security検証用の保護テストEPは1本のみ許可。今後#21/#8/#14のAC詳細化時にこの境界を踏襲する。
- 2026-08-15（Sprint3計画フェーズ、#18）: #18（E5サインオン/サインオフ）はbackend REST＋Spring Security基盤に絞る方針を確定。フロント責務（Vueログイン画面・Piniaauthストア・ルーターガードでの元URL復帰・SPA配送）は#24（E6フロント土台・Sprint5予定）へAC7（サインオン/サインオフUI）・AC8（元URL復帰・復帰先は相対のみ／SBD-9）・AC-neg2として持ち越し。次回#24 RefinementでPOがSP・AC文言を最終確定する。**#18側のAC1/AC3のbackendスコープ限定文言修正・フロント責務#24委譲済み旨の備考明記は、Sprint3中に完了済み**（`backlog/sprint_03/sprint_backlog.md` に反映確認）。
- 2026-08-15（Sprint4計画フェーズ、#20）: レート制限/ロックアウトはDB表保持を採用（`jpetstore-database`にFlyway追加、cross-repo）。決定論的テスト・stateless/水平スケール整合・新規依存ゼロを重視し、in-memory/Redisは見送り。キーはper-username（per-IPは見送り）、閾値は5回連続失敗/15分ロック（env上書き可）をデフォルトとした。
- 2026-08-15（Sprint4計画フェーズ、#20）: 既定資格情報プリフィル廃止のうちフロント責務分（ログイン画面初期値）は#24へAC化して持ち越し。backend/シード側は既達（`R__test_user.sql`にj2ee/j2ee等を含まない）。
- 2026-08-15（Sprint4計画フェーズ、#20）: オープンリダイレクト対策（SBD-9）はbackendにredirectのsinkが存在しないため予防的allowlistガードを新設せず、sink不在の明文化＋回帰テストで固定する構造的解消とした。フロントの元URL復帰allowlist/相対チェックは#24既存AC8に持ち越し済み（重複回避を確認）。
- 2026-08-15（Sprint4計画フェーズ、#21）: 認可土台のスコープを「本人スコープ（所有者一致）の再利用可能な部品＋実証（SecuredPingController相当）」に限定。ロールモデル（ADMIN取得経路）整備・各ドメインへの実適用は後続の各ドメインStoryへ委譲（#23と同じ土台Story規律を踏襲）。
- 2026-08-15（Sprint4計画フェーズ、#21）: AC2（認可失敗監査記録）は既存のrecordAuthzFailure機構（401/403の3経路・テスト済）で達成済みと判定。#21では確認とドメイン認可失敗が同一経路に乗ることの実証に留め、新規記録インフラは作らない。
- 2026-08-15（Sprint5計画フェーズ、#24）: 認証状態のリロード再水和は backend に `GET /api/auth/me` を追加して対応（cross-repo）。DEV推奨案（メモリのみ保持・持ち越し）からユーザー判断で変更。
- 2026-08-15（Sprint5計画フェーズ、#24）: AC8（保護ルート復帰）の実証は router ガード＋復帰先バリデータの純関数Vitestで完結させる。ライブの保護デモ画面は不要とし、各ドメインStoryで`meta.requiresAuth`接続時に実証する。
- 2026-08-15（Sprint5計画フェーズ、#24）: 画像アセットはfoundation=hero.png/logo.svgのみ。product/category計24枚は各ドメインStoryで追加する（未使用アセット回避）。
- 2026-08-15（Sprint5計画フェーズ、#24）: i18nはvue-i18n v11を採用、本Storyでは英語のみ実装。キー構造は`domain.context.key`。日本語化は#25へ委譲。
- 2026-08-15（Sprint5計画フェーズ、#24）: 共通コンポーネントは既存`.jps-*`CSS活用＋新規AppHeader/AppLayoutの2つのみに限定し、過剰な共通化を回避する。
- 2026-08-15（Sprint5計画フェーズ、#24）: 本スプリントで作る画面はHome（i18n英語化）＋Signon＋Signoffのみ。ドメイン画面（E1〜E4）は各Storyへ委譲する。
- 2026-08-16（Sprint6計画フェーズ、#1）: アイテム詳細の在庫バッジ「残少」閾値をN=5（`0<qty≤5`=残少／`qty>5`=在庫あり／`qty≤0`=在庫切れ）で確定。レガシーは全在庫qty=10000固定で残少概念なし＝純粋な新UX値。閾値は定数/config化する。
- 2026-08-16（Sprint6計画フェーズ、#1）: 在庫ステータスの実装方式はm_code区分値登録を採用（DEV推奨の手書きenumをユーザーがオーバーライド）。ユーザー一般方針「区分値は基本的にm_codeに登録する」を明示。architecture-conventions §3.1が在庫ステータスをm_code候補として例示しつつ「確定はPO/仕様で」としていた点を、今回PO/仕様の確定としてm_code採用と判断。code_type新設＋既存ジェネレータでTS定数生成（database→frontend）、backendの区分値enumは手書きでm_codeに整合。在庫statusはqtyからの算出値（`StockStatus.of(qty)`）で格納列は持たず、qtyのレスポンス非露出は維持。
- 2026-08-16（Sprint6計画フェーズ、#1）: 一覧APIのページ採番は1-index（`page=1`始まり）で確定。レスポンス`PageResponse<T> { content, page, size, totalElements, totalPages }`を規約化し、#2/#9が踏襲する先例とする。
- 2026-08-16（Sprint6計画フェーズ、#1）: `spec/intended-diff-ledger.md` にID-28（在庫状況3段階バッジ表示。旧＝在庫概念なし固定qty→新＝在庫あり/残少[閾値N=5]/在庫切れのバッジ表示・qty非露出）を追記。一方、在庫ステータスのm_code化とページ採番1-indexは、外部から観測される仕様挙動（バッジの見え方・ページングの結果）を変えないfrontend/backendの実装規約選択と判定し、台帳追記は不要とした。
- 2026-08-16（Sprint7計画フェーズ、#2）: 検索語のLIKEメタ文字（`%`/`_`）はハードニング方針を採用。旧＝ワイルドカードとして機能→新＝リテラルとして一致（ESCAPE併用）。予測可能性・意図せぬ全件マッチ防止のため。SBD-17（SQLi対策）は維持。`spec/intended-diff-ledger.md`にID-29として追記済み（Sprint7 Retroで実施）。
- 2026-08-16（Sprint7計画フェーズ、#2）: AC1「カテゴリフィルタは任意（PO決定）」を今スプリントで実装することを確定。API `categoryId`は任意パラメータ、UIにカテゴリ選択を追加する。
- 2026-08-16（Sprint7計画フェーズ、#2）: 検索APIのパスは`/api/products/search`を採用（既存`/api/products/**` permitAllに含まれSecurityConfig変更不要。path-var`{id}`より優先されるためルーティング衝突なし）。今後の検索系エンドポイントの先例規約とする。
- 2026-08-16（Sprint7計画フェーズ、#2）: カテゴリフィルタUIは検索結果画面に配置（ヘッダは検索バーのみ）。
- 2026-08-16（Sprint7計画フェーズ、#2）: 検索対象列は`name`/`category_id`/`description`のみとし、カテゴリ表示名（`m_category.name`）は対象外（レガシー`product.category`FK列＝新`category_id`に忠実で[L2]旧同値維持）。
- 2026-08-16（Sprint7計画フェーズ、#2）: 不明な`categoryId`によるフィルタは空配列＋200（フィルタ扱い）とし、404にはしない。
- 2026-08-16（Sprint8計画フェーズ、#4）: サーバーカートの永続スコープをDB永続サーバーカート（3-repo）で確定。`database`に`t_cart`/`t_cart_item`（`V00_000_010`〜）を新設、`backend`で永続化3層＋ログイン時マージ、`frontend`は未ログイン`localStorage`保持。backendがSTATELESS/JWTのため「サーバーカート」はDBテーブルが実質必須（in-memory/session不可）。所有者は`m_account.user_id`をFK。
- 2026-08-16（Sprint8計画フェーズ、#4）: ログイン時マージの数量意味論を「数量加算・在庫数で上限クランプ」で確定（例: client 2個＋server 3個→在庫≥5なら5個・在庫3なら3個にクランプ）。新規挙動のため`spec/intended-diff-ledger.md`のID-19を「クライアント保持＋ログイン時にサーバーカートへマージ（client+server数量加算・在庫数で上限クランプ）」へ具体化した（Sprint8 Retroで反映）。
- 2026-08-16（Sprint8計画フェーズ、#4・D1）: 未ログイン（匿名）カートの在庫上限検証は、公開の冪等`GET /api/items/{itemId}/orderable?quantity=N`（orderable真偽＋reasonのみ返却・在庫数は非露出）を新設して対応。qty非露出（ID-28）を維持し、既存permitAll `GET /api/items/**`に収まるためSecurityConfig変更不要。次回Refinementで#4のAC5/AC-neg1または備考への反映を検討する。
- 2026-08-16（Sprint8計画フェーズ、#4・D2）: カート画面ルートは公開ルート（`meta.requiresAuth`なし）で確定。AC3で未ログインもクライアントカートを閲覧するため。認証必須は`/api/cart/**` APIのみ。次回Refinementで#4のAC3備考への反映を検討する。
- 2026-08-16（Sprint8計画フェーズ、#4・D3）: `t_cart_item`にversion列は付与しない方針で確定。単一所有者・暫定カート（在庫の実権威はE3のガード付き原子減算）でlast-write-win許容、加算はUPSERTでアトミック。backend-conventions §4.3（更新系エンティティにversion）は多編集者エンティティの後勝ち防止（409）が目的のため、単一所有者カートには適用外の意図的逸脱と判断。
- 2026-08-16（Sprint9計画フェーズ、#6）: AC2「非数値quantity→400」実現のため`HttpMessageNotReadableException`→400のグローバル例外ハンドラを新設。既存catch-allで500になっていた不正JSONボディをSBD-10準拠で400に正規化する（全EPに影響するが厳密改善・副作用なしと判断。この1ハンドラのみ・グローバルJackson設定は変更しない）。
- 2026-08-16（Sprint9計画フェーズ、#6）: XSRF-TOKEN CookieのSecure/SameSite設定は既存`jwt.cookie.secure`/`jwt.cookie.same-site`設定を再利用して統一する（ハードコードは不採用。環境設定可・非localhost HTTP環境でのfootgun回避）。
- 2026-08-16（Sprint9計画フェーズ、#6）: 数量バリデーションはupdate=DTO層（boxed Integerで欠落と明示0を区別）・merge=サービス層throw（blank-itemId無視契約とBean Validation順序の衝突回避）の非対称配置を採用。
- 2026-08-16（Sprint9計画フェーズ、#6）: Origin/SameSite検証はSameSite属性＋トークンで充足すると判定し、新規Originチェックフィルタは追加しない。
- 2026-08-16（Sprint9計画フェーズ、#5）: updateで数量0を指定した場合はカート項目削除として維持（既存挙動どおり）。mergeで数量≤0が指定された場合は400とする。
- 2026-08-16（Sprint10計画フェーズ、#7）: #7 AC1のプリフィル住所データ源は、backendにread-only住所返却API `GET /api/account/me`（cross-repo新設）とする。E4（#13/#14）着手前で上流提供者が不在という制約への対応。支払/courierは既存ID-8/ID-21で被覆済みのため対象外。
- 2026-08-16（Sprint10計画フェーズ、#7）: #7/#8のスコープ境界は「確定前段（内容確認画面まで）=#7」「送信・在庫引当・永続化=#8」で確定。#7ではorder送信API（orderApi）は作らず、accountApi（GETプリフィル）のみとする。
- 2026-08-16（Sprint10計画フェーズ、#7）: ウィザード下書き状態の保持方式はPinia（メモリのみ・揮発）で確定。legacy session `workingOrderForm`の永続方式からの意図的な変更（ウィザード状態管理のモダン化・単純化）。`spec/intended-diff-ledger.md`にID-30として追記した（Sprint10 Retroで実施）。
- 2026-08-16（Sprint10計画フェーズ、#7）: プリフィル住所APIの構成は`GET /api/account/me`・`AccountController`/`AccountApplicationService`/`AccountContactCustomEntity`/`Mapper`/`AccountContact`で確定。#14（E4アカウント編集）が同APIを再利用する可能性がある先例規約。
- 2026-08-16（Sprint10計画フェーズ、#7）: 空カートで注文フォームへ進入した場合の誘導UXは`/cart?reason=empty-checkout`へreplace＋CartViewで正規化エラー表示（別画面新設なし）で確定。
- 2026-08-16（Sprint10計画フェーズ、#7）: ウィザードのルート構成は`/checkout`単一ルート＋内部ステップで確定（下書き揮発方式との整合を優先し、deep-link事故を回避）。
- 2026-08-17（Sprint11計画フェーズ、#8）: 在庫不足・同時発注競合負け（ガード付きUPDATE affected rows==0）による注文失敗は409 Conflictとする。既存の楽観ロック競合（OptimisticLockConflictException=409）と系統を揃える。
- 2026-08-17（Sprint11計画フェーズ、#8）: 空カートでの確定リクエスト到達時も409 Conflictとする（在庫不足と同じ「注文失敗」系統に統一）。
- 2026-08-17（Sprint11計画フェーズ、#8）: 注文完了画面のスコープは最小（注文番号＋合計＋サンクスメッセージのみ）で確定。明細行・商品名の一覧表示は#10（注文詳細閲覧。ID-24も#10）へ委譲する。
- 2026-08-17（Sprint11計画フェーズ、#8）: 確定時にマスター価格がカート表示時と変動していた場合は再計算値でそのまま確定する。差分検知・再確認フローは実装しない（SBD-2カート価格サーバ権威の帰結、as-is等価）。
- 2026-08-17（Sprint11計画フェーズ、#8）: 在庫不足・空カートによる注文失敗は監査ログに記録する（成功・失敗の両方。失敗は`@Transactional(REQUIRES_NEW)`別txで`result=FAILURE`のORDER_CREATEを記録）。AC6「結果」は成功のみでなく失敗も含む解釈とした（DEVの当初デフォルト『成功時のみ』からユーザー判断で変更）。
- 2026-08-17（Sprint11計画フェーズ、#8）: 支払プレースホルダ／email・phoneの扱いは既存決定を踏襲。requestに支払フィールドを設けない（F3.6・ID-8）。email・phoneはt_orderに列が無く注文レベルでは非永続（既存ID-21等と整合）。
- 2026-08-17（Sprint11計画フェーズ、#8）: 在庫減算行のWHO（update_program/update_user_id）付与は単一POJO引数でupdate_user_id明示＋update_programはInterceptor自動付与とする（@Param複数だとInterceptor自動付与が効かないため）。
- 2026-08-17（Sprint11 Retro、#8）: `spec/intended-diff-ledger.md`のID-22説明文を強化（「固定プレースホルダ・状態変更は監査ログに記録」に「注文作成の成功・失敗いずれもORDER_CREATEイベントとして記録し、失敗時はresult=FAILUREとする」旨を追記）。新規の旧新差分ではなく既存宣言の実装詳細を明確化する変更。
- 2026-08-17（Sprint12計画フェーズ、#28/29/30 Refinement）: 実コード確認でdrift実態を確定（Cart/Catalog/Account/Order=Mapper直呼びあり、Auth=Mapper/Entity直呼び無し）。domainのRepository interfaceは未存在で#29が初回PoC。
- 2026-08-17（Sprint12計画フェーズ、#28）: N+1解消Issueは#29に畳み込まず単独fallbackとして温存。Ready=NotReady・SP=2。#29完了後の新`MyBatisCartRepository`にバッチ取得を実施する。
- 2026-08-17（Sprint12計画フェーズ、#29優先順位）: E4（#13〜17）着手前にRepository層のテンプレを確立するため、既存Readyの E3（#9〜12）より前に割り込む。Ready=Ready・SP=5。
- 2026-08-17（Sprint12計画フェーズ、#30スコープ是正）: 対象をCatalog/Account/Order（Orderは最重の違反源だが当初漏れていたため追加）に確定し、Auth（drift無し）を除外。Ready=Ready・SP=8・#29完了後着手。
- 2026-08-17（Sprint12計画フェーズ、運用ルール）: 今後の新規StoryはRepository経由で実装し、新規driftの発生を止める。#30は既存分のretrofitに純化する。E4（#13〜17）Refinement時に各Issueへこの方針を反映する。
- 2026-08-17（Sprint12計画フェーズ、#29・D1）: Cart集約の型戦略は単一`Cart`/`CartItem`型のrich化を採用。`stockQuantity`はprivate内部保持・外向きは射影アクセサのみ公開してID-28（qty非露出）を型で強制する。書込集約と読取read-modelを物理分離する完全CQRS（案B）は型/Converter二重化でYAGNIと判断し不採用。
- 2026-08-17（Sprint12計画フェーズ、#29・D2）: `CartRepository`の書込メソッド面は、細粒度`upsertItem`/`removeItem`をドメイン語彙メソッドとして残す方針を採用（1ユースケース=1行書込・余分な書込ゼロ）。集約全体差分の`save(Cart)`一本化は不採用。
- 2026-08-17（Sprint13計画フェーズ、#30・Q1）: Order集約はrichな集約を新設せず、#8のorchestration（原子引当・item_id固定順・all-or-nothing・監査）をOrderApplicationServiceに残す方針（案A）を採用。過剰抽象（YAGNI）回避。
- 2026-08-17（Sprint13計画フェーズ、#30・Q2）: read/write Repositoryの命名は統一`...Repository`を採用（DEV推奨のread=`...Query`／write=`...Repository`分離をユーザーがオーバーライド）。`CatalogRepository`/`AccountRepository`等、read/write問わず`...Repository`。意味論（CQRS射影）は不変。
- 2026-08-17（Sprint13計画フェーズ、#30・Q3）: `InventoryRepository`は新規`domain/inventory`パッケージ（`domain/cart`と並行構造）に配置する方針で確定。
- 2026-08-17（Sprint14計画フェーズ、#9）: `t_order`へのページング用複合索引`(user_id, order_id)`追加に伴い、既存単一索引`idx_t_order_user_id`は置換（ADD+DROP）を採用。
- 2026-08-17（Sprint14 Retro）: SM長期記憶Sprint1トレンド（FK明示索引の一貫性）とPO質問ログSprint14（複合索引導入時の単一索引重複排除）が「`rules/database.md`に索引設計方針が未文書化」という同一文書ギャップの2件目の再発と判定し、PO内チェックリストではなく`rules/database.md`に「索引ハイジーン」節を新設してfirmupした（PO判断・ユーザー確認なしで実施。#skills-changelogへ[PO]で投稿）。
- 2026-08-17（Sprint15計画フェーズ、#11）: remoting/WS getOrder廃止（security）は構造的に既達（sink不在等・ID-5で既に「廃止」宣言済み）と判定し、成果物は回帰テスト＋明文化で固定する（Sprint4 SBD-9型を踏襲）。新規実装は行わない。
- 2026-08-17（Sprint15計画フェーズ、#12）: 支払プレースホルダ文言を「扱わない」明示（`This demo does not collect or store card details.`系）に変更する。現行の「将来追加」ニュアンス（`Payment details will be added in a future update.`）はID-8の意図（カード情報を扱わない/保持しない）と方向が異なるため是正。`spec/intended-diff-ledger.md`のID-8記述を強化済み（Sprint15 Retroで実施）。
- 2026-08-17（Sprint15計画フェーズ、#12）: 未参照の孤立カード型定義（frontend enum/定数）は撤去する。ただしDBのm_code 0002区分値は温存する（cross-repo回避・codegenの参照元のため）。
- 2026-08-17（Sprint15計画フェーズ、#12）: backend `CardType.java`は削除しない（温存）。m_code 0002からのcodegen生成物と判明し、削除してもgenerateEnums手動実行で再生成されるため実質的に削除不可能。frontend定数のみ撤去する。
- 2026-08-17（Sprint15計画フェーズ、#28）: カートマージのバッチ化後も重複itemId入力時の挙動を厳密に不変とする（coalesceで対応）。バックログ前提「localStorageはitemIdキーmap」は誤りで実体は配列、dedupは書込ロジック側が担保する仕様と確定（refactor ACの「挙動不変」を厳守）。
- 2026-08-17（Sprint15 Retro）: Sprint12傾向2（リファクタ/是正Story起票時のスコープが実コード未裏取りでずれる）を、Sprint15 #12（CardType.javaのcodegen生成物由来未確認）・#28（localStorageのデータ構造前提の誤り）の2件の再発を受け、対象を「スコープ」から「前提（データ構造・削除対象の生成物由来等）」まで含む形に拡張してチェックリストへ正式昇格した。
- 2026-08-17（Sprint16計画フェーズ、#13・E1）: 登録レート制限（AC-neg2/SBD-6）はDB-backed登録試行テーブル`t_register_attempt`（既存`t_login_attempt`と同方式・IPキー・XFF非信頼）を採用。
- 2026-08-17（Sprint16計画フェーズ、#14・E2）: `m_account.version`を単一楽観ロックトークンとし、account版ガード→409／profileは同一tx内無ガード更新（account失敗時のtxロールバックでlost update防止）。二重version非採用。
- 2026-08-17（Sprint16計画フェーズ、#14・E3）: 編集用readは新規`GET /api/account`（version込み）を追加。チェックアウト用の既存`GET /api/account/me`は無変更。
- 2026-08-17（Sprint16計画フェーズ、#13・E7）: 登録WHOのcreate_user_id（未認証guestアクター）はNULL（architecture-conventions §2.1準拠）。
- 2026-08-17（Sprint16計画フェーズ、#13・E8）: 登録エンドポイントは`POST /api/register`独立パスを採用。`/api/account/**`は一律authenticatedに保つ。
- 2026-08-17（Sprint16 Retro）: `spec/architecture-conventions.md`にD7（secure-by-default系の試行カウンタ・レート制限状態はDB-backedで永続化）を新設・firmupした。Sprint4 #20（`t_login_attempt`）とSprint16 #13（`t_register_attempt`）の12スプリントを隔てた再発を受け、Sprint14の「DB索引ハイジーン」節新設と同型の対応としてspec側に一般原則を明文化（実装詳細はbackend-conventions/DEV裁量に委譲）。
- 2026-08-17（Sprint16 Retro）: メール検証（#13 AC4/SBD-6のプレースホルダ部分）を別バックログIssue **#32**「[E4] 登録時のメールアドレス所有権を検証する（メール確認リンク）」として起票（Ready=NotReady・deferred）。Sprint15 #12支払プレースホルダと同型の持ち越し運用。
- 2026-08-17（Sprint16 Retro）: `spec/intended-diff-ledger.md`のID-11を拡張し、登録試行への列挙対策（レート制限）を関連Story#13として追加（`t_register_attempt`・D7参照）。一方、メール検証プレースホルダ自体はlegacyも元々持たない機能であり実装差分が生じていないため、新規IDは起票せず#32着手時に再判定することとした。
- 2026-08-18（Sprint17計画フェーズ、#15）: パスワード変更AC-neg1「現在パスワード誤り時のレスポンス」を422 Unprocessable Entity（新設`InvalidCurrentPasswordException`。Spring Framework 7.0での呼称変更に伴い実装は`HttpStatus.UNPROCESSABLE_CONTENT`を使用・値=422は不変）で確定。状態変更APIのエラー系統を三分離：現在PW誤り=422／弱PW・不正入力=400（Bean Validation）／真の未認証=401（httpClient silent-refresh経路）／CSRF欠落=403。401はhttpClient silent-refresh+retryの誤発火、403はCSRF欠落403との意味重複を避けるため、既存API語彙で未使用の422を選定した。
- 2026-08-18（Sprint17 Retro）: `spec/intended-diff-ledger.md`への追記なし。#15/#16/#17はいずれも既存security-baseline宣言（SBD-16/SBD-3/SBD-5）の実装・既達確認の範疇で、新規のエラーコード配分・CSRF回帰実証・入力検証強化は実装レベルの詳細でありlegacy比較上の新規「旧→新」観測可能差分エントリを要しないと判定した（Sprint13型の「内部実装リファクタ・API仕様不変」判定と同型）。
- 2026-08-18（Sprint18計画フェーズ、#34・Q1）: ログイン後ナビ導線の配置形態は案A-1（`AppHeader.vue`の`.app-header__account`クラスタ内・greetingとSign Outの間にOrders/Accountを認証時のみ平置き）で確定。既存`jps-navlink`/`--jps-*`/i18n再利用、ドロップダウン化（案B）は不採用。理由＝新規インタラクティブ状態ゼロで最小コスト・低回帰・既存トークン完全再利用・Sprint7のAppHeaderテスト衝突教訓と整合。
- 2026-08-18（Sprint18計画フェーズ、#34・Refinement 2026-08-18②由来）: AC4「認証済み時はhomeヒーロー『New Here?』→/registerを非表示」を確定ACへ昇格し#34に畳み込み（別Issue分離・ドロップは不採用）。同一領域（AppHeader/認証時ナビ整合）のため低コスト内包。
- 2026-08-18（Sprint18計画フェーズ、#33・Q2）: 保護ルートの実機到達性バグ（リロード/直リンクでsignonへ誤誘導）は、`main.ts`の`app.use(router)`を再水和`await`の後ろへ移動して解消。`pinia`/`i18n`のinstallは現行位置のまま維持（`useAuthStore()`呼び出しに必要なため）。
- 2026-08-18（Sprint18計画フェーズ、#31・Q3）: Null type safety警告（4ファイル6件）は該当メソッド参照を全てラムダ式へ書き換えて解消（1案確定・Sprint4教訓の複数案割れ回避）。null解析のグローバル無効化は行わない（AC4）。
- 2026-08-18（Sprint18計画フェーズ、#26・Q4/Refinement 2026-08-18②由来）: 依存更新トリガ基準は「EOLまたは重大CVEが確認された依存のみ更新」で確定。棚卸し結果、`mysql-connector-j`のみCVE-2023-22102（HIGH）が確認され`8.0.33→26.7.0`へ更新。他5依存（Flyway/Spock/Testcontainers等）は重大CVE・EOLともに未確認のため据え置き。スコープは`jpetstore-database`の依存のみに確定（`jpetstore-backend`の主要依存は対象外＝将来Issue）。
- 2026-08-18（Sprint18 Retro）: `spec/intended-diff-ledger.md`のID-26を拡張し、関連Storyへ#26を追加・具体的な更新内容（`mysql-connector-j`のCVE-2023-22102対応・8.0.33→26.7.0）を明記した。#33/#34/#27/#31は台帳追記なしと判定：#33/#34は既に出荷済みのfeature Story（#7/#9/#10/#14/#15）の実装完全性を復旧する不具合修正（legacyには「SPAのハードロード到達性」という比較対象概念自体が無く、新規の「旧→新」意図差分の宣言には該当しない）、#27/#31は挙動不変が明示されたAC（#27 AC2〜4・#31 AC2）を持つ内部実装/ビルド設定リファクタ（Sprint13型判定と同型）。
