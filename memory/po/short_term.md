# PO 短期記憶（今スプリント）

## Sprint 22 Retro（2026-08-21）で棚卸し済み

Sprint21 Retro以降の文脈（#51計画フェーズのQ1〜Q7・C1〜C4の質問ログ11件、Sprint ReviewでのSMからの明示報告2件〔R8aのexpectation変更・AC7ゲート値再合意案〕へのPO判断、Issue #51本文の訂正要否の確定）は本Retroで棚卸し済み。要点は`memory/po/long_term.md`（Sprint22セクション追記・傾向1「リファクタ/是正・棚卸し系Story起票時の前提裏取り」の適用範囲をL2 parity/特性化テストStoryへ拡張・決定ログ6件追記）に反映済み。

**本Retroで実施した確定作業**:
- **PO判断1（承認）**: R8aのexpectation変更（Issueスコープ表の`EQUIVALENT`→`INTENDED_DIVERGENCE(ID-24)`）。台帳ID-24・design doc側の更新内容を確認済み。
- **PO判断2（承認）**: AC7ゲート値再合意案（BRANCH≥28/34=82.4%・INSTRUCTION≥1360/1424=95.5%・`gate`→`gate-v2`分母切替）。`out2`/`out3`の`gate-v2`jacoco.csvをPOが直接パースして検算済み。
- `reports/after/l2-parity-coverage.md` §6「ゲート値の再合意案（PO提示）」→「（PO合意済み）」へ本Retroで更新（検算根拠を追記）。
- `spec/intended-diff-ledger.md`のID-14・ID-24、`spec/l2-parity-design.md`§2・R2/F5の内部矛盾是正（Sprint21 Retro申し送り分）がいずれも#51で解消済みであることを確認（追加対応不要）。

## Sprint 23（2026-08-21〜）#52 L4 台帳照合 — PO 分担分（実施済み）

DEV と分担（実測＝テストの名指しは DEV／**仕様側の判定は PO**）。成果物は `backlog/sprint_23/po-ledger-classification.md`（ブランチ `docs/52-l4-ledger-verification`・コミットは SM がまとめて実施）。

- **判定内訳（最終・33件）**: **観測可能 31件／構造的に観測不能 2件（ID-6・ID-31）／観測不要 0件**。初版は 31 件（ID-1〜31）で観測可能 29 件だったが、**ID-32・ID-33 の追記（下記）で 33 件**になった。`観測可能` は「観測点がある」ではなく「**比較が成立する**」の意。DEV の実測と突き合わせて観測点を名指しできなかった ID が「穴」になる。
- **判定は台帳・spec の記述ではなく legacy/新の現物で裏取りした**（#52 AC3）。legacy 側は `Item.jsp` / `Cart.java` / `ViewCategoryAction.java` / `SqlMapSequenceDao.java` / `SignonController.java` / `remoting-servlet.xml` / hsqldb schema 等、新側は Flyway DDL・`SecurityConfig.java`・`CartView.vue` を実際に開いた。
- **AC6（台帳の ID 順整列）完了**: ID-31 を ID-30 の直後へ移動。`awk`（CRLF 保持）で行移動し、**行数53→53・バイト数12450→12450・sort 後の md5 一致・`git diff` は同一行の1挿入1削除のみ**で「並べ替えのみ」を機械確認済み。
- **PO 判定が Issue #52 AC2 の例示と食い違う点が1つ**: AC2 は ID-27（多言語）を「観測不要」の例に挙げるが、**PO は観測可能と判定**した。理由は下記「未解決の判断事項」参照。
- **SM から追加依頼された未解決台帳事項3件も処理済み**（出所はいずれも `reports/after/l3-security-regression-backend.md`。SM のメッセージは転記せず該当節を開いて確認）。
  - **【PO 判断を出した】L3 §3-5 在庫枯渇（denial-of-inventory）**: **台帳追記は不要**。旧のカード必須検証は `OrderValidator#validateCreditCard` の `rejectIfEmpty` 3本（メッセージが文字どおり `"FAKE (!) credit card number required."`）＝**任意の1文字で通るダミー欄**なので、**旧にも決済ゲートは存在せず**「認証済みユーザーが在庫を減らし放題」は旧新で変わらない＝差分ではない。よって **ID-8 の帰結として ID-8 に受容明記するのも不正確**（ID-8 は当該能力を新たに作っていない）。**記録するなら ID-1 側が正確**（「在庫0が他の顧客をブロックする」という影響の質は**在庫ガードを入れた ID-1 の帰結**。旧はガードが無く在庫マイナスでも注文が通るため枯渇が他者をブロックしない）。**リスクは現時点で受容**（旧に対する退行ではない／認証済みアカウントが必要／決済が無く金銭的損失なし・復旧可能／状態変更APIのレート制限は ID-11 の適用範囲外）。**本番デプロイ基盤・本番プロファイルの整備決定時に再評価**（#45・#43(B)(E)・#46 の再判定と同じタイミングに束ねる）。
  - **【未台帳差分の候補へ】L3 §3-6 注文の二重送信**: 旧側にも冪等制御が無いため（`PetStoreImpl.java:147-150` の `insertOrder` は insert→updateQuantity のみ）**旧新差分ではない公算が高く**、台帳行ではなく「冪等キー導入 or 受容」のプロダクト判断。旧はむしろ GET リンク（ID-9）＋在庫ガード無し（ID-1）で二重送信がより起きやすく被害も大きい。
  - **【食い違いではなかった】ID-26 の根拠**: 台帳が言うのは `jpetstore-database` の 8.0.33→26.7.0、L3 §2.2 が「非該当」と言うのは backend の 9.5.0（`jpetstore-backend/build.gradle:65,68` vs `jpetstore-database/build.gradle:30,38` で現物確認）。**対象アーティファクトが別なので矛盾しない**（8.0.33 は「8.2.0 で修正済」より前＝該当）。実在する未記録は **§3-7 の版乖離**（同一ライブラリが 9.5.0 / 26.7.0 の2版で固定されているのに ID-26 の「版固定」宣言に記録が無い）。
  - **【ID-22 は「穴」ではなく「宣言と実装の乖離」】**: N3（`placeOrder` の catch が `InsufficientStockException` のみ＝他の失敗は 500 で失敗監査が残らない）と S20-4（best-effort catch が共通 `insert` に置かれ**成功系も fail-open 化**）で、ID-22 の「成功・失敗いずれも記録」が**部分未達**。観測点の有無とは別軸なので L4 のゲート判定では分けて扱うよう SM へ申し送り済み。
- **【重要】台帳本文の訂正・追記を実施した（2026-08-21・ユーザー承認済み・SM 経由）**。AC6（並べ替えのみ）の達成を確定させたうえで、**別フェーズ**として実施。**台帳は 31 行 → 33 行**。
  - **as-is/after の訂正3件**: ID-27 as-is（日英 JSP 同梱 → **アカウント設定だけで実体なし**）・ID-28 as-is（在庫状況表示なし → **生の在庫数をそのまま表示**）・ID-10 after（セッション再生成 → **STATELESS のため実体は新規 JWT 発行**・趣旨は不変）。
  - **新規追記2件**: **ID-32**（認証状態の保持方式＝HTTPセッション → 完全ステートレス JWT Cookie）・**ID-33**（カート一覧のページング廃止＝旧4件/頁 → 全件表示。ID-20 がカタログ一覧限定であることを行内明示）。位置づけは「**決定自体は実装時に済んでおり記録だけが抜けていた**」。
  - **行内追記2件**: **ID-1**（denial-of-inventory を受容済みの帰結として記録）・**ID-26**（`mysql-connector-j` の版乖離 backend 9.5.0 / database 26.7.0 を据え置き記録。**理由は未記録・要確認**と明記）。
  - **見送り1件**: 注文の二重送信は台帳に載せない（**条件付き保留**＝実測で「旧は二重送信で1件しか作らない」等の反証が出たら行に切り替える）。
  - **台帳末尾に `## 訂正履歴` 節を新設**し、旧記述・訂正後の要点・根拠 `file:line`・訂正日・訂正者(PO)・発見経路(L4 #52)・見送った候補の保留条件をすべて記録した。**次に台帳を触る人はこの節を先に読むこと。**
  - **差分の限定を機械確認**: `git diff -U0` で変更行は ID-1・ID-10・ID-26・ID-27・ID-28・ID-31(AC6の移動)・ID-32・ID-33 の8行＋訂正履歴節のみ。**他25行は1文字も変わっていない**。CRLF も維持（`file` で確認）。
  - **CRLF の落とし穴**: Git Bash の `awk`/`sed` はこのリポジトリの CRLF ファイルを LF に落とす。行移動・行挿入を script でやるときは `ORS="\r\n"` 指定か、最後に `sed -i 's/\r$//; s/$/\r/'` で復元し、**`file` コマンドで CRLF を確認**すること（`grep -c $'\r$'` は Bash ツール経由だと当てにならない）。これを怠ると全行が diff に出る。

## 未解決の判断事項

- **【Sprint23・解決済み】ID-27 の as-is 誤りは訂正済み**。#52 AC2 は ID-27（多言語）を「観測不要」の例に挙げていたが、PO は **観測可能**と判定した（旧の `profile.langpref` は描画に一切影響せず、新は #25〔closed 2026-08-18〕で言語切替が実際に効くため「同じ設定値に対して描画が変わるか」の比較が今も成立する。#25 完了が閉じたのは**新側内部のスコープ差**であって旧新差分ではない）。**前提だった as-is 記述の誤り（日英 JSP 同梱）は 2026-08-21 に訂正済み**なので、以後この行を「観測不要」と誤読する余地は無い。
- **【Sprint23・解決済み】台帳記述の要訂正4件はすべて実施**（B-1 ID-27 as-is／B-2 ID-28 as-is／B-3 ID-10 after／B-4 ID-26 の版乖離）。詳細と根拠は台帳の `## 訂正履歴` 節にある。**B-4 の「据え置き理由が未記録」だけは残件**（backend 9.5.0 を据え置いた判断の記録が無い。ID-26 の更新基準「EOL または重大CVE確認時のみ更新」を適用した結果である可能性が高いが未確認）。次に依存を触る Story で確認して埋めること。
- **【Sprint23・解決済み】在庫枯渇（denial-of-inventory）は ID-1 の行内に受容記録を追記**（新規 ID は起こさない＝旧にも決済ゲートが無く旧新差分ではないため）。SM verification で補強: 旧の `OrderValidator` は **Struts 経路からは呼ばれない**（`NewOrderAction` が検証を挟まず `insertOrder` を直呼び。参照は Spring MVC 経路の `OrderFormController.java:87` のみ）＝稼働 legacy では1文字の非空チェックすら走っていない。**残る宿題は再評価のタイミング**＝本番デプロイ基盤・本番プロファイルの整備決定時に #45・#43(B)(E)・#46 と束ねて再判定する（状態変更 API のレート制限を将来 Story の候補として扱う）。
- **【Sprint23・条件付き保留】注文の二重送信は台帳に載せない**（2026-08-21 ユーザー判断）。旧にも冪等制御が無く（`PetStoreImpl.java:147-150`）、旧は GET リンク（ID-9）＋在庫ガード無し（ID-1）でより二重送信しやすいため旧新差分として成立しない公算が高い。**実測で「旧は二重送信で1件しか作らない」等の反証が出たら台帳行に切り替える**。保留条件は台帳 `## 訂正履歴` の「見送った候補」にも記録済み。
- 確認メール（#8備考。注文確定完了時の受領メール送付）を独立の将来Feature Issue として NotReady で track するか（現状は#8備考に記録のみ。#8自体はクローズ済みだが、この将来Feature化の判断自体は未決着のため継続）。
- **#42〜#47 は 2026-08-20 の Refinement で NotReady 確定・塩漬け（SP は全件設定済み）**。Ready 昇格のトリガは、#43(B)(E)・#45＝本番デプロイ基盤／本番プロファイルの整備決定、#42・#44・#43(C)(D)・#46・#47＝Phase 4 完了後に容量が空いた時点。**#44／#46／#47 は `AuditLogRecorder` 周辺を共通で触るため、昇格させるなら同時期に束ねるのが自然**（着手順は #47(B)(C) の境界整理 → #46 のキー複合化 → #44 の結線 → #47(A)）。
- **#46 の重大度は本番デプロイ基盤の整備決定時に再判定**する（quota キーが `getRemoteAddr()` 固定のため、リバースプロキシ/LB 配下では全クライアントが単一バケットを共有し「per-IP なので他人の監査は消せない」という Low 格下げ根拠が崩れる）。#45／#43(B)(E) と同じタイミングで見直す。
- **#47(C) 完了後の Retro で、台帳 ID-22 の関連Story欄拡張の要否を判定する**（#47 は現状 NotReady・未着手のため未実施。Sprint20 の ID-25/ID-11 と同型の運用）。
- **`reports/after/l3-security-regression-backend.md` §1 回帰表の判定（S10「是正」・S15「消滅＋是正」・S17「消滅」）は、Sprint20で#38/#39/#41が実装・マージされたことにより実効性が回復した見込みだが、正式な回帰再実証（L3再実行）はまだ行っていない**。Phase 4 の合否ゲート（L3・L4）判定および `reports/after/verification-report.md` 作成時に、SEC側での回帰再検証結果と実装内容を突き合わせる必要がある（SM/PO の L4 作業として継続監視）。
- #32（メール検証・Sprint16 Retro新規起票）は現状NotReady（deferred）。実SMTP基盤・検証トークン表の着手判断が出た時点でReady昇格の要否を判断する。
- **#51のIssue Body（Refinement確定版）に4箇所の訂正が必要（次回Refinementでユーザーに確認のうえ実施）**：(1) AC2「W5: アカウント編集 — 2ケース」→3ケース（W5a/W5b/W5c）、(2) R8bの理由説明文「新側は404」→403、(3) AC-neg4「`POST /api/login`」→「`POST /api/auth/login`」、(4) スコープ表のR8a期待値「`EQUIVALENT`」→「`INTENDED_DIVERGENCE(ID-24)`」（本Retroで承認したPO判断1に伴う訂正）。(1)〜(3)はSprint22計画フェーズでSM/DEVが実コード・実測値に当たって判明（C1〜C3）。(4)はSprint ReviewでのSM報告を受け本Retroで確定。**なお「到達可能上限30/34→28/34」はIssue本文には元々記載が無かった数値（`backlog/sprint_22/sprint_backlog.md`というSM作業物側の記載）のため、Issue本文の訂正対象には含めない**（本Retroで`mcp__github__get_issue`により実物確認済み）。

## SMやDevから受けた質問ログ

| 日付 | 質問者 | 質問内容 | 対応 | バックログ修正が必要か |
|---|---|---|---|---|
